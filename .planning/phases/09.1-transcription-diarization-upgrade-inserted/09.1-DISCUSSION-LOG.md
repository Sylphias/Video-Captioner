# Phase 9.1: Transcription & Diarization Upgrade - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-26
**Phase:** 09.1-transcription-diarization-upgrade-inserted
**Areas discussed:** Transcription model, Diarization strategy, Cross-platform paths, Output compatibility

---

## Transcription Model

| Option | Description | Selected |
|--------|-------------|----------|
| faster-whisper on CUDA | Same library, switch device='cuda' + compute_type='float16'. Minimal change. | |
| WhisperX | Wrapper with forced phoneme alignment for better word timestamps. | |
| NVIDIA Parakeet TDT | NeMo-based, optimized for NVIDIA GPUs. Fast but newer word timestamp support. | ✓ (primary) |

**User's choice:** Parakeet TDT as primary, WhisperX as fallback if Parakeet doesn't work out
**Notes:** User wants to test Parakeet first via validation spike before committing

---

## Diarization Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| pyannote on CUDA | Same pipeline, switch to torch.device('cuda'). Proven accuracy. | |
| NeMo MSDD | NVIDIA Multi-Scale Diarization Decoder. Native CUDA, same ecosystem as Parakeet. | ✓ |
| WhisperX built-in | Bundles pyannote internally. Only relevant if WhisperX is the transcriber. | |

**User's choice:** NeMo MSDD
**Notes:** Pairs with Parakeet in the NeMo ecosystem

---

## Cross-Platform Paths

| Option | Description | Selected |
|--------|-------------|----------|
| Windows-only going forward | Drop Mac code, simplify for Windows + CUDA only. Clean break. | ✓ |
| Cross-platform (Mac + Windows) | Auto-detect platform and GPU. More code, keeps both working. | |
| Windows primary, Mac best-effort | Optimize for Windows, don't actively break Mac. | |

**User's choice:** Windows-only going forward
**Notes:** Clean break from Mac

---

## Output Compatibility

| Option | Description | Selected |
|--------|-------------|----------|
| Same format, extend only | Keep existing structure, new fields allowed. Zero downstream breakage. | ✓ |
| Allow restructuring | Adapt format if new models output differently. More work. | |
| Strict identical output | Byte-for-byte compatible. Maximum safety. | |

**User's choice:** Same format, extend only
**Notes:** Existing fields must stay, new fields allowed

---

## Claude's Discretion

- NeMo/Parakeet dependency management approach
- Validation spike structure and pass/fail criteria
- Whether scripts stay separate or merge
- Windows setup commands
- Progress reporting format adaptation

## Deferred Ideas

None
