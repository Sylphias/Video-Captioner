---
phase: 06-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/remotion-composition/package.json
  - packages/frontend/package.json
  - packages/shared-types/src/index.ts
  - packages/remotion-composition/src/types.ts
  - packages/remotion-composition/src/fonts.ts
  - packages/remotion-composition/src/index.ts
  - packages/remotion-composition/src/SubtitleOverlay.tsx
  - packages/remotion-composition/src/SubtitleComposition.tsx
  - packages/remotion-composition/src/Root.tsx
  - packages/frontend/src/store/subtitleStore.ts
  - packages/frontend/src/components/PreviewPanel.tsx
  - packages/frontend/src/hooks/useRender.ts
  - packages/backend/src/routes/render.ts
autonomous: true

must_haves:
  truths:
    - "StyleProps includes strokeColor, strokeWidth, verticalPosition, and fontFamily fields"
    - "SubtitleCompositionProps includes speakerStyles record for per-speaker overrides"
    - "TranscriptPhrase includes optional dominantSpeaker field"
    - "SubtitleOverlay renders text with WebkitTextStroke when strokeWidth > 0"
    - "SubtitleOverlay positions text using top + translateY(-50%) based on verticalPosition"
    - "SubtitleOverlay merges per-speaker style overrides with global style for the active phrase"
    - "PreviewPanel memoizes inputProps with useMemo to prevent Player re-renders"
    - "PreviewPanel passes dominantSpeaker and speakerStyles in inputProps"
    - "useRender sends speakerStyles and dominantSpeaker in render payload"
    - "Backend render route accepts speakerStyles in RenderBody"
    - "8 curated Google Fonts loaded at module level via @remotion/google-fonts"
    - "Zustand store has speakerStyles record, setSpeakerStyle, and clearSpeakerStyle actions"
  artifacts:
    - path: "packages/remotion-composition/src/fonts.ts"
      provides: "Module-level Google Font loading with curated list"
      exports: ["FONTS", "FontName", "FONT_NAMES", "getFontFamily"]
    - path: "packages/remotion-composition/src/types.ts"
      provides: "Extended StyleProps with stroke/position, SpeakerStyleOverride, AnimationType"
      exports: ["StyleProps", "SpeakerStyleOverride", "AnimationType", "SubtitleCompositionProps"]
    - path: "packages/shared-types/src/index.ts"
      provides: "TranscriptPhrase with optional dominantSpeaker"
      contains: "dominantSpeaker"
    - path: "packages/remotion-composition/src/SubtitleOverlay.tsx"
      provides: "Subtitle rendering with stroke, vertical position, and per-speaker style merge"
    - path: "packages/frontend/src/store/subtitleStore.ts"
      provides: "speakerStyles state and actions"
      contains: "setSpeakerStyle"
  key_links:
    - from: "packages/remotion-composition/src/fonts.ts"
      to: "packages/remotion-composition/src/SubtitleComposition.tsx"
      via: "import side-effect (module-level loadFont calls)"
      pattern: "import.*fonts"
    - from: "packages/frontend/src/store/subtitleStore.ts"
      to: "packages/frontend/src/components/PreviewPanel.tsx"
      via: "Zustand selector for speakerStyles"
      pattern: "useSubtitleStore.*speakerStyles"
    - from: "packages/frontend/src/hooks/useRender.ts"
      to: "packages/backend/src/routes/render.ts"
      via: "POST body with speakerStyles"
      pattern: "speakerStyles"
    - from: "packages/remotion-composition/src/SubtitleOverlay.tsx"
      to: "packages/remotion-composition/src/types.ts"
      via: "SpeakerStyleOverride merge at render time"
      pattern: "speakerStyles"
---

<objective>
Extend the data layer, composition rendering, and render pipeline to support full subtitle styling: stroke, vertical position, per-speaker style overrides, curated Google Fonts, and animation type field. Wire everything from Zustand store through PreviewPanel to server render.

Purpose: This plan builds the complete data and rendering foundation that Plan 06-02's UI controls will write to. Without this, style controls have nothing to bind to.
Output: Extended types, working composition rendering with all style properties, memoized preview, and updated render pipeline that propagates per-speaker styles to the MP4 output.
</objective>

<execution_context>
@/Users/eigenair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eigenair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-styling/06-RESEARCH.md
@packages/shared-types/src/index.ts
@packages/remotion-composition/src/types.ts
@packages/remotion-composition/src/SubtitleOverlay.tsx
@packages/remotion-composition/src/SubtitleComposition.tsx
@packages/remotion-composition/src/Root.tsx
@packages/remotion-composition/src/index.ts
@packages/frontend/src/store/subtitleStore.ts
@packages/frontend/src/components/PreviewPanel.tsx
@packages/frontend/src/hooks/useRender.ts
@packages/backend/src/routes/render.ts
@packages/frontend/src/lib/grouping.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, extend types, and create fonts module</name>
  <files>
    packages/remotion-composition/package.json
    packages/frontend/package.json
    packages/shared-types/src/index.ts
    packages/remotion-composition/src/types.ts
    packages/remotion-composition/src/fonts.ts
    packages/remotion-composition/src/index.ts
  </files>
  <action>
    1. Install dependencies:
       - In packages/remotion-composition: `npm install @remotion/google-fonts@4.0.379 --save-exact`
       - In packages/frontend: `npm install react-colorful`

    2. Extend `TranscriptPhrase` in packages/shared-types/src/index.ts:
       - Add optional `dominantSpeaker?: string` field to the existing `TranscriptPhrase` interface

    3. Extend `StyleProps` in packages/remotion-composition/src/types.ts:
       - Add `strokeColor: string` (default '#000000')
       - Add `strokeWidth: number` (px, 0 = no stroke)
       - Add `verticalPosition: number` (0-100, percentage from top)
       - Keep existing: `highlightColor`, `baseColor`, `fontSize`, `fontFamily`

    4. Add new types to packages/remotion-composition/src/types.ts:
       - `SpeakerStyleOverride = Partial<StyleProps> & { animationType?: AnimationType }`
       - `AnimationType = 'none' | 'pop' | 'slide-up' | 'bounce'` (union type per research recommendation)
       - Extend `SubtitleCompositionProps` to add `speakerStyles: Record<string, SpeakerStyleOverride>`

    5. Create packages/remotion-composition/src/fonts.ts:
       - Import loadFont from 8 specific @remotion/google-fonts subpaths: Inter, Roboto, Montserrat, Oswald, Lato, Poppins, NotoSans, PlayfairDisplay
       - Call each loadFont at module level with params: 'normal', { weights: ['400', '700'], subsets: ['latin'] }
       - NOTE: Check the actual loadFont API signature — research shows `loadFont(style, options)` but some @remotion/google-fonts versions use `loadFont()` with no args or `loadFont({ weights, subsets })`. Read the installed package's type definition to confirm the correct signature before writing the calls.
       - Export FONTS object mapping font names to loadFont() return values
       - Export FontName type (keyof typeof FONTS)
       - Export FONT_NAMES array (Object.keys(FONTS) as FontName[])
       - Export getFontFamily(name: FontName): string function that returns FONTS[name].fontFamily

    6. Update packages/remotion-composition/src/index.ts barrel:
       - Add export for SpeakerStyleOverride, AnimationType types from './types'
       - Add export for FontName, FONT_NAMES, getFontFamily from './fonts'
       - Import './fonts' to ensure module-level side effects fire (font loading)
  </action>
  <verify>
    cd packages/remotion-composition && npx tsc --noEmit
    cd packages/shared-types && npx tsc --noEmit
  </verify>
  <done>
    StyleProps has 7 fields (highlightColor, baseColor, fontSize, fontFamily, strokeColor, strokeWidth, verticalPosition). SpeakerStyleOverride and AnimationType are exported. TranscriptPhrase has optional dominantSpeaker. fonts.ts loads 8 Google Fonts at module level and exports getFontFamily. All types compile clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update composition rendering with stroke, position, and per-speaker merge</name>
  <files>
    packages/remotion-composition/src/SubtitleOverlay.tsx
    packages/remotion-composition/src/SubtitleComposition.tsx
    packages/remotion-composition/src/Root.tsx
  </files>
  <action>
    1. Update SubtitleOverlay.tsx:
       - Change props interface to accept `speakerStyles: Record<string, SpeakerStyleOverride>` alongside existing `phrases` and `style`
       - Import SpeakerStyleOverride from './types'
       - After finding the activePhrase, look up `dominantSpeaker` from the phrase (activePhrase.dominantSpeaker — TranscriptPhrase now has this optional field) and merge: `const override = dominantSpeaker ? (speakerStyles[dominantSpeaker] ?? {}) : {}; const effectiveStyle: StyleProps = { ...style, ...override }`
       - Replace `bottom: '10%'` with `top: \`${effectiveStyle.verticalPosition}%\``, add `transform: 'translateY(-50%)'`
       - Add `WebkitTextStroke: effectiveStyle.strokeWidth > 0 ? \`${effectiveStyle.strokeWidth}px ${effectiveStyle.strokeColor}\` : undefined` to the container div style
       - Keep existing textShadow on word spans for additional legibility
       - Use `effectiveStyle.fontSize`, `effectiveStyle.fontFamily`, `effectiveStyle.highlightColor`, `effectiveStyle.baseColor` instead of `style.*` for all rendering

    2. Update SubtitleComposition.tsx:
       - Import './fonts' at the top (triggers module-level font loading side effect)
       - Destructure `speakerStyles` from props alongside existing `videoSrc`, `phrases`, `style`
       - Pass `speakerStyles` to SubtitleOverlay

    3. Update Root.tsx:
       - Update DEFAULT_PROPS to include the new StyleProps fields: strokeColor: '#000000', strokeWidth: 2, verticalPosition: 80
       - Add `speakerStyles: {}` to DEFAULT_PROPS
  </action>
  <verify>
    cd packages/remotion-composition && npx tsc --noEmit
  </verify>
  <done>
    SubtitleOverlay renders with stroke, vertical position via top+translateY, and merges per-speaker overrides. SubtitleComposition imports fonts.ts and passes speakerStyles through. Root.tsx default props include all 7 style fields + empty speakerStyles. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend store, memoize preview, and fix render pipeline</name>
  <files>
    packages/frontend/src/store/subtitleStore.ts
    packages/frontend/src/components/PreviewPanel.tsx
    packages/frontend/src/hooks/useRender.ts
    packages/backend/src/routes/render.ts
  </files>
  <action>
    1. Update subtitleStore.ts:
       - Import SpeakerStyleOverride and AnimationType from '@eigen/remotion-composition'
       - Add `speakerStyles: Record<string, SpeakerStyleOverride>` to the store interface and initialize to `{}`
       - Add `setSpeakerStyle: (speakerId: string, override: SpeakerStyleOverride) => void` action — merges override into existing entry: `{ ...state.speakerStyles, [speakerId]: { ...state.speakerStyles[speakerId], ...override } }`
       - Add `clearSpeakerStyle: (speakerId: string) => void` action — deletes the key from the record
       - Update DEFAULT_STYLE to include: strokeColor: '#000000', strokeWidth: 2, verticalPosition: 80, fontFamily: 'Inter'
       - Update `reset` action to also clear speakerStyles to `{}`

    2. Update PreviewPanel.tsx:
       - Import `useMemo` from React
       - Add Zustand selector for `speakerStyles`: `const speakerStyles = useSubtitleStore((s) => s.speakerStyles)`
       - Wrap inputProps construction in `useMemo` with dependencies `[jobId, session, style, speakerStyles]`
       - In the memoized inputProps, map phrases to include `dominantSpeaker`: `session.phrases.map((p) => ({ words: p.words, dominantSpeaker: p.dominantSpeaker }))`
       - Add `speakerStyles` to the inputProps object

    3. Update useRender.ts:
       - Read `speakerStyles` from the Zustand store alongside `session` and `style` inside the render callback: `const { session, style, speakerStyles } = useSubtitleStore.getState()`
       - Map phrases to include `dominantSpeaker`: `.map((p) => ({ words: p.words, dominantSpeaker: p.dominantSpeaker }))`
       - Add `speakerStyles` to the POST body: `JSON.stringify({ phrases, style, speakerStyles })`

    4. Update backend render.ts route:
       - Import `SpeakerStyleOverride` from '@eigen/remotion-composition'
       - Extend `RenderBody` interface to add `speakerStyles: Record<string, SpeakerStyleOverride>`
       - Destructure `speakerStyles` from req.body alongside `phrases` and `style`
       - Add `speakerStyles` to the `inputProps` object passed to `dispatchRender`: `{ videoSrc, phrases, style, speakerStyles }`
       - Default `speakerStyles` to `{}` if not provided: `const speakerStyles = (req.body as RenderBody).speakerStyles ?? {}`
  </action>
  <verify>
    cd /Users/eigenair/projects/personal/eigen-video-editor && npx tsc --noEmit --project packages/frontend/tsconfig.json && npx tsc --noEmit --project packages/backend/tsconfig.json
  </verify>
  <done>
    Zustand store has speakerStyles record with setSpeakerStyle/clearSpeakerStyle actions and extended DEFAULT_STYLE. PreviewPanel memoizes inputProps including speakerStyles and dominantSpeaker. useRender sends speakerStyles + dominantSpeaker in render POST body. Backend render route accepts and passes speakerStyles through to the worker. Full TypeScript compilation passes across frontend and backend.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in all three packages (shared-types, remotion-composition, frontend, backend)
2. `npm run dev` starts without errors (both frontend and backend)
3. StyleProps type has exactly 7 fields: highlightColor, baseColor, fontSize, fontFamily, strokeColor, strokeWidth, verticalPosition
4. SubtitleCompositionProps has exactly 4 fields: videoSrc, phrases, style, speakerStyles
5. TranscriptPhrase has optional dominantSpeaker field
6. fonts.ts exports FONT_NAMES array with 8 entries
7. SubtitleOverlay uses `top` + `translateY(-50%)` (not `bottom`)
8. SubtitleOverlay applies WebkitTextStroke when strokeWidth > 0
9. PreviewPanel wraps inputProps in useMemo
10. useRender POST body includes speakerStyles
</verification>

<success_criteria>
All type extensions compile. Composition renders subtitles with stroke and configurable vertical position. Per-speaker style overrides merge at render time in SubtitleOverlay. Preview and render pipelines propagate the full style + speakerStyles + dominantSpeaker data. No un-memoized inputProps in PreviewPanel.
</success_criteria>

<output>
After completion, create `.planning/phases/06-styling/06-01-SUMMARY.md`
</output>
