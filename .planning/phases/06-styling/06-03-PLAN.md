---
phase: 06-styling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/pages/SubtitlesPage.tsx
  - packages/frontend/src/pages/SubtitlesPage.css
  - packages/frontend/src/components/StageTabBar.tsx
  - packages/frontend/src/components/StageTabBar.css
  - packages/frontend/src/components/SpeakersStage.tsx
  - packages/frontend/src/components/SpeakersStage.css
  - packages/frontend/src/components/PreviewPanel.tsx
  - packages/frontend/src/components/PreviewPanel.css
autonomous: true

must_haves:
  truths:
    - "User sees 4 stage tabs (Text, Timing, Speakers, Styling) instead of old Transcript/Style tabs"
    - "All 4 tabs are clickable and switch the editor panel content"
    - "Speakers tab shows diarize controls and speaker reassignment UI"
    - "Styling tab shows existing StylePanel and SpeakerStylePanel"
    - "Preview panel has a collapse/expand toggle button"
    - "Collapsed preview gives more vertical space to the editor area"
    - "Preview panel defaults to visible"
  artifacts:
    - path: "packages/frontend/src/components/StageTabBar.tsx"
      provides: "4-stage tab navigation component"
      min_lines: 25
    - path: "packages/frontend/src/components/SpeakersStage.tsx"
      provides: "Speaker assignment stage wrapping diarize + reassignment UI"
      min_lines: 40
    - path: "packages/frontend/src/pages/SubtitlesPage.tsx"
      provides: "Restructured page with StageTabBar and stage-conditional rendering"
  key_links:
    - from: "packages/frontend/src/pages/SubtitlesPage.tsx"
      to: "packages/frontend/src/components/StageTabBar.tsx"
      via: "import and render"
      pattern: "StageTabBar"
    - from: "packages/frontend/src/pages/SubtitlesPage.tsx"
      to: "packages/frontend/src/components/SpeakersStage.tsx"
      via: "conditional render when activeStage === 'speakers'"
      pattern: "SpeakersStage"
    - from: "packages/frontend/src/components/PreviewPanel.tsx"
      to: "collapse state"
      via: "collapsed prop or internal state"
      pattern: "collapsed|collapse"
---

<objective>
Replace the current Transcript/Style tab bar with a 4-stage workflow navigation (Text, Timing, Speakers, Styling), make the preview panel collapsible, and rewire existing Stage 3 (Speakers) and Stage 4 (Styling) components into the new structure.

Note: Requirements STYLE-01 through STYLE-05 (design tokens, dark theme, component styling foundations) were implemented in Plans 06-01 and 06-02. This plan builds the stage navigation shell on top of that foundation.

Purpose: This is the foundational shell plan — all subsequent plans (Text Editor, Timing Editor) plug into the stage navigation created here. Stage 3 and Stage 4 use existing components (diarize controls, speaker reassignment, StylePanel, SpeakerStylePanel) so they work immediately.

Output: Working 4-tab stage navigation with Speakers and Styling stages functional, collapsible preview panel, placeholder content for Text and Timing stages.
</objective>

<execution_context>
@/Users/eigenair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eigenair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-styling/06-CONTEXT.md
@.planning/phases/06-styling/06-01-SUMMARY.md
@.planning/phases/06-styling/06-02-SUMMARY.md
@packages/frontend/src/pages/SubtitlesPage.tsx
@packages/frontend/src/pages/SubtitlesPage.css
@packages/frontend/src/components/PreviewPanel.tsx
@packages/frontend/src/components/PreviewPanel.css
@packages/frontend/src/components/TranscriptEditor/TranscriptEditor.tsx
@packages/frontend/src/components/StylePanel/StylePanel.tsx
@packages/frontend/src/components/StylePanel/SpeakerStylePanel.tsx
@packages/frontend/src/hooks/useDiarize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StageTabBar and SpeakersStage components</name>
  <files>
    packages/frontend/src/components/StageTabBar.tsx
    packages/frontend/src/components/StageTabBar.css
    packages/frontend/src/components/SpeakersStage.tsx
    packages/frontend/src/components/SpeakersStage.css
  </files>
  <action>
Create StageTabBar.tsx — a horizontal tab bar component that renders 4 stage tabs:
1. "Text" (id: 'text')
2. "Timing" (id: 'timing')
3. "Speakers" (id: 'speakers')
4. "Styling" (id: 'styling')

Props: `activeStage: StageId`, `onStageChange: (stage: StageId) => void`
Export type: `type StageId = 'text' | 'timing' | 'speakers' | 'styling'`

Per user decision: All 4 tabs are always available (not gated). Add a subtle visual indicator (e.g., a small dot or slightly brighter text) on the "suggested next stage" — for now, always highlight the stage immediately after the active one. This is purely visual — users can click any tab freely.

Style the tab bar (StageTabBar.css) to match the existing dark theme:
- Horizontal layout, `gap: 0`, bottom border matching `--color-border`
- Active tab: `--color-text-primary` text, `--color-accent-green` underline (2px bottom border), `font-weight: 500`
- Inactive tab: `--color-text-secondary` text, transparent bottom border
- Suggested next tab: slightly brighter text (`--color-text-primary` at 70% opacity) or a small green dot
- Use same padding/sizing as existing tab bar styles in SubtitlesPage.css

Create SpeakersStage.tsx — wraps the existing speaker detection and reassignment functionality into a stage-specific view:
- Import and render diarize controls (speakers count input + diarize button) currently inline in SubtitlesPage
- Import and render the speaker legend from TranscriptEditor
- Render a phrase list showing phrases with speaker badges, allowing reassignment via dropdown (reuse PhraseRow's speaker badge pattern)
- Accept props: `jobId: string`, `seekToTime: (timeSec: number) => void`, `diarizeState: DiarizeState`, `diarize: () => void`, `numSpeakers: number`, `setNumSpeakers: (n: number) => void`
- Use Zustand store for session, speakerNames, reassignWordSpeaker

Style SpeakersStage.css:
- Speaker legend at top
- Diarize controls below legend
- Phrase list showing speaker-colored phrases below
- Match existing dark theme, use CSS custom properties from tokens.css
  </action>
  <verify>
`npx tsc --noEmit --project packages/frontend/tsconfig.json` passes. Both component files exist and export their expected types/components.
  </verify>
  <done>
StageTabBar renders 4 clickable tabs with active state indicator. SpeakersStage wraps speaker detection + reassignment UI into a self-contained stage component. Both compile without TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure SubtitlesPage with stage navigation and collapsible preview</name>
  <files>
    packages/frontend/src/pages/SubtitlesPage.tsx
    packages/frontend/src/pages/SubtitlesPage.css
    packages/frontend/src/components/PreviewPanel.tsx
    packages/frontend/src/components/PreviewPanel.css
  </files>
  <action>
**PreviewPanel changes:**
Add a `collapsed` prop (boolean) and `onToggleCollapse` callback prop. When collapsed:
- The Player is hidden (unmount or `display: none`)
- Show a thin collapsed bar (height ~32px) with a "Show preview" button/icon
- When expanded (default), show the Player as before plus a collapse button (small icon in the corner, e.g., chevron-up or minimize icon — use a unicode character like "▴" to avoid adding a dependency)

Add corresponding CSS in PreviewPanel.css:
- `.preview-panel--collapsed`: thin bar style
- `.preview-panel__collapse-btn`: positioned top-right of the preview, subtle, visible on hover

**SubtitlesPage restructure:**
Replace the entire "transcribed" state rendering block in SubtitlesPage.tsx:

1. Replace `activeTab` state (`'transcript' | 'style'`) with `activeStage` state using the new `StageId` type. Default to `'text'`.

2. Add `previewCollapsed` state (boolean, default: false).

3. Replace the old tab bar JSX with `<StageTabBar activeStage={activeStage} onStageChange={setActiveStage} />`.

4. In the top section, wrap PreviewPanel with collapsed/onToggleCollapse props. When collapsed, the top section should shrink to the collapsed bar height.

5. Move diarize controls OUT of the top-controls area. They now live inside the SpeakersStage component. Pass `diarizeState`, `diarize`, `numSpeakers`, and `setNumSpeakers` from SubtitlesPage's `useDiarize()` hook as explicit props to `<SpeakersStage />`. These are local hook state values that SpeakersStage cannot access on its own — they MUST be passed as props:
   ```tsx
   <SpeakersStage
     jobId={uploadState.jobId!}
     seekToTime={seekToTime ?? (() => {})}
     diarizeState={diarizeState}
     diarize={diarize}
     numSpeakers={numSpeakers}
     setNumSpeakers={setNumSpeakers}
   />
   ```

6. Keep the following in the top-controls area: "Go to subtitle" button, render controls (render button, progress bar, download link). These are cross-stage actions that are always visible.

7. In the editor section, conditionally render based on activeStage:
   - `'text'`: For now, render a placeholder div with text "Text Editor — coming in Plan 04". Also render the existing TranscriptEditor as a temporary fallback so the app remains functional.
   - `'timing'`: Placeholder div "Timing Editor — coming in Plan 05"
   - `'speakers'`: Render `<SpeakersStage />` with the props listed in step 5 above
   - `'styling'`: Render `<StylePanel />` + `<SpeakerStylePanel />` (same as current Style tab)

8. Remove the old tab bar CSS classes (`subtitles-page__tab-bar`, `subtitles-page__tab`, `subtitles-page__tab--active`) from SubtitlesPage.css — they're replaced by StageTabBar.css.

9. Keep "Re-transcribe" and "Upload another video" buttons at the bottom of the editor scroll area.

**CSS updates to SubtitlesPage.css:**
- Remove old tab bar styles (replaced by StageTabBar)
- Add `.subtitles-page__top--collapsed` with reduced height (e.g., 40px) when preview is collapsed
- Ensure the editor scroll area expands to fill available space when preview is collapsed
  </action>
  <verify>
`npx tsc --noEmit --project packages/frontend/tsconfig.json` passes. Run `just dev` (or equivalent) and visually confirm in the browser:
1. Four stage tabs appear (Text, Timing, Speakers, Styling)
2. Clicking each tab switches the editor content
3. Speakers tab shows diarize controls and speaker UI
4. Styling tab shows the style controls
5. Preview panel has a collapse button that hides the video player
6. Render controls are still visible and functional
  </verify>
  <done>
SubtitlesPage renders 4-stage tab navigation. Speakers and Styling stages show functional existing components. Preview panel is collapsible. Text and Timing stages show placeholder content with the existing TranscriptEditor visible on the Text stage as a temporary fallback. The app compiles and is fully functional. diarizeState/diarize/numSpeakers/setNumSpeakers are explicitly passed from SubtitlesPage's useDiarize() hook to SpeakersStage as props.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project packages/frontend/tsconfig.json` — TypeScript compiles
- All 4 stage tabs render and are clickable
- Speakers stage shows diarize controls + speaker legend + phrase speaker assignment
- Styling stage shows StylePanel + SpeakerStylePanel
- Preview panel collapse/expand works
- Render and download controls remain accessible across all stages
- No regressions: existing editing, diarize, render, download flows still work
</verification>

<success_criteria>
- 4-stage tab navigation replaces old Transcript/Style tabs
- Speakers tab shows functional diarize + speaker reassignment UI
- Styling tab shows functional style controls with live preview
- Preview is collapsible (defaults to visible)
- All cross-stage controls (render, download, go-to-subtitle) remain accessible
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-styling/06-03-SUMMARY.md`
</output>
