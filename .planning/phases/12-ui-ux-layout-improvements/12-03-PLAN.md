---
phase: 12-ui-ux-layout-improvements
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - packages/frontend/src/pages/SubtitlesPage.tsx
  - packages/frontend/src/pages/SubtitlesPage.css
autonomous: false
requirements: [D-01, D-02, D-03, D-05]

must_haves:
  truths:
    - The toolbar between preview and editor is organized into labelled logical button groups with visual separators, not a flat row of buttons
    - Button groups follow the D-02 order and clustering with compact consistent sizing
    - The full restructured UI (layout, panels, tabs, toolbar) passes a human visual check
  artifacts:
    - packages/frontend/src/pages/SubtitlesPage.css
  key_links:
    - subtitles-page__toolbar-group and subtitles-page__toolbar-separator structure the toolbar
    - Go-to-subtitle leads the first group; Render/Download and re-ingest actions are separated clusters
---

<objective>
Reorganize the toolbar between the preview and the editor into logical button groups with visual separators and consistent compact sizing, following the D-02 grouping. Then run the end-of-phase human verification covering all of Phase 12's layout, panel, tab, and toolbar changes.

Purpose: Deliver D-01, D-02, D-03 and the Go-to-subtitle portion of D-05, then confirm the whole phase visually.
Output: Grouped toolbar markup + CSS in SubtitlesPage, and a human-verified Phase 12 UI.

This plan runs in Wave 2 because it edits the same SubtitlesPage toolbar that Plan 12-01 restructures (it depends on 12-01 having removed the Global Styling button and established the center-column layout). Plan 12-02's work (tabs, SRT button) completes in Wave 1 and is included in this plan's final human-verify.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ui-ux-layout-improvements/12-CONTEXT.md
@.planning/phases/12-ui-ux-layout-improvements/12-RESEARCH.md
@.planning/phases/12-ui-ux-layout-improvements/12-PATTERNS.md
@.planning/phases/12-ui-ux-layout-improvements/12-01-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan adds two CSS classes already listed in 12-01's phase-wide artifacts: `subtitles-page__toolbar-group` and `subtitles-page__toolbar-separator`. No new TypeScript symbols or files. (See 12-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase list.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add toolbar group and separator CSS</name>
  <files>
    packages/frontend/src/pages/SubtitlesPage.css (modify)
  </files>
  <read_first>
    - packages/frontend/src/pages/SubtitlesPage.css (current `.subtitles-page__top-controls` flat-row rule at ~lines 52-58; `.subtitles-page__goto-btn` compact button reference at ~lines 264-278)
    - .planning/phases/12-ui-ux-layout-improvements/12-RESEARCH.md (Pattern 4 toolbar group/separator CSS)
    - .planning/phases/12-ui-ux-layout-improvements/12-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Add two new rules to `SubtitlesPage.css` (D-01, D-03). `.subtitles-page__toolbar-group { display: flex; align-items: center; gap: var(--spacing-xs); }` groups related buttons with tight spacing. `.subtitles-page__toolbar-separator { width: 1px; height: 20px; background: var(--color-border); flex-shrink: 0; align-self: center; }` is the vertical divider between major clusters (Claude's-discretion separator style per CONTEXT.md). Keep the existing `.subtitles-page__top-controls` flex row but reduce its inter-child spacing so groups (not individual buttons) carry the visual gap — set its `gap` to `var(--spacing-sm)` (down from `--spacing-md`) so grouped buttons read tighter than the cluster gaps. For D-03 compact consistency, ensure the toolbar button classes share the compact bordered pattern already established by `.subtitles-page__goto-btn` (transparent bg, 1px solid border, 4px radius, `var(--spacing-xs) var(--spacing-md)` padding, `font-size: var(--font-size-sm)`); if any existing toolbar button rule (undo-btn, save-btn, time-shift-btn, render-btn, download-btn, toolbar-btn) declares a larger padding or fixed height that breaks visual consistency, normalize it to that compact pattern. Do not change button colors semantically (keep the muted variant for "Upload new" and the dirty variant for Save). Use only existing CSS tokens.
  </action>
  <verify>
    <automated>cd packages/frontend && npx tsc --noEmit -p tsconfig.json && grep -q "subtitles-page__toolbar-separator" src/pages/SubtitlesPage.css && grep -q "subtitles-page__toolbar-group" src/pages/SubtitlesPage.css</automated>
  </verify>
  <acceptance_criteria>
    - Both new classes exist: `grep -c "subtitles-page__toolbar-group\|subtitles-page__toolbar-separator" packages/frontend/src/pages/SubtitlesPage.css` returns at least 2.
    - The separator is a 1px vertical divider: `grep -A5 '.subtitles-page__toolbar-separator {' packages/frontend/src/pages/SubtitlesPage.css` shows `width: 1px` and `background: var(--color-border)`.
    - Frontend typecheck exits 0.
  </acceptance_criteria>
  <done>Toolbar group and separator CSS exist and match the compact bordered visual language; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap toolbar buttons into logical groups per D-02</name>
  <files>
    packages/frontend/src/pages/SubtitlesPage.tsx (modify)
  </files>
  <read_first>
    - packages/frontend/src/pages/SubtitlesPage.tsx (the `subtitles-page__top-controls` block — after 12-01 the Global Styling button and StyleDrawer are already removed; buttons present: Go to subtitle, Undo/Redo, Save, Time Shift group, Render/Download, Re-transcribe, Replace Video + hidden input, Upload new, plus render progress/error blocks)
    - packages/frontend/src/pages/SubtitlesPage.css (the toolbar-group/toolbar-separator classes added in Task 1)
    - .planning/phases/12-ui-ux-layout-improvements/12-CONTEXT.md (D-02 exact group order, D-05 goto placement)
    - .planning/phases/12-ui-ux-layout-improvements/12-PATTERNS.md (toolbar group boundary line references)
  </read_first>
  <action>
    Inside `subtitles-page__top-controls`, wrap the existing toolbar buttons into `subtitles-page__toolbar-group` divs following the D-02 order and insert `subtitles-page__toolbar-separator` divs at the two cluster boundaries marked by the pipes in D-02. Target structure in order:
    - Editing cluster (no internal vertical dividers, just group gaps): Group A = Go-to-subtitle (leading, per D-05 discretion to keep it in the first group) + the existing Undo/Redo controls; Group B = Save button (when `projectId`); Group C = the Time Shift label/input/Apply/Reset controls.
    - `subtitles-page__toolbar-separator` (first pipe).
    - Group D = Render MP4 + Download MP4.
    - `subtitles-page__toolbar-separator` (second pipe).
    - Group E = Re-transcribe + Replace Video (with its hidden file input) + Upload new.
    Keep the render progress bar and render error blocks where they are (after Group E or at the end of the toolbar) — they are status output, not grouped buttons. Do not change any button's onClick, disabled logic, labels, or the existing `subtitles-page__save-btn--dirty` / `subtitles-page__toolbar-btn--muted` modifiers — this is pure markup wrapping (D-01, D-02). Preserve conditional rendering (Save only when `projectId`, Download only when render succeeded). Do not reintroduce the removed Global Styling button.
  </action>
  <verify>
    <automated>cd packages/frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Toolbar groups are present in markup: `grep -c "subtitles-page__toolbar-group" packages/frontend/src/pages/SubtitlesPage.tsx` returns at least 5.
    - Exactly two cluster separators are placed: `grep -c "subtitles-page__toolbar-separator" packages/frontend/src/pages/SubtitlesPage.tsx` returns 2.
    - The removed Global Styling button stays removed: `grep -c "subtitles-page__styling-btn\|Global Styling" packages/frontend/src/pages/SubtitlesPage.tsx` returns 0.
    - Button behavior preserved: Re-transcribe, Replace Video, Upload new, Render MP4, Undo, Redo, and Apply/Reset onClick handlers still present (`grep` confirms each label/handler still in file).
    - Frontend typecheck exits 0.
  </acceptance_criteria>
  <done>Toolbar buttons are wrapped into the D-02 groups with two separators and compact sizing; all handlers and conditional rendering preserved; typecheck passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the full Phase 12 UI restructure</name>
  <action>Pause for human visual verification. Present the what-built summary and how-to-verify steps below; do not proceed until the user responds with the resume signal. This is a blocking checkpoint — no code changes occur here.</action>
  <what-built>
    The complete Phase 12 UI restructure: (12-01) three-column full-height layout with a permanent Lane panel (left) and permanent Global Style panel (right) flanking a center column, StyleDrawer overlay removed, right panel swapping between Global / phrase / speaker with a back-to-global control; (12-02) polished stage tabs and a compact Upload SRT button; (12-03) the reorganized grouped toolbar.
  </what-built>
  <how-to-verify>
    1. Start the app: run `just dev` from the repo root, then open the frontend (per current networking, e.g. http://localhost:5173 or the WSL host URL noted in STATE.md).
    2. Open an existing saved project (or upload + transcribe a short clip) to reach the editing layout.
    3. Layout (D-13/D-14/D-15): confirm the Lane panel is a full-height column on the left and the Style panel is a full-height column on the right, with preview + toolbar + editor in the center. Confirm the Lane panel stays visible on all three stages — Text Edit, Word Timing, Animation. Confirm each side panel collapses via its toggle button and is fixed-width (not drag-resizable). Confirm the right panel starts expanded.
    4. Global styling (D-06/D-07/D-08): confirm Global Styling controls are visible by default in the right panel, there is no overlay drawer/backdrop, and there is no "Global Styling" button in the toolbar.
    5. Override swap (D-09/D-10/D-11/D-12): click a phrase (Text Edit) and a speaker (Word Timing) — the right panel content swaps to Phrase/Speaker override controls. Use the panel's back/close control to return to Global Styling. Confirm clicking elsewhere in the editor does NOT auto-return to Global.
    6. Tabs (D-04): confirm the three stage tabs read as proper tabs — active tab clearly highlighted, inactive tabs muted, consistent sizing; the first tab reads "Text Edit".
    7. Toolbar (D-01/D-02/D-03): confirm the toolbar between preview and editor is grouped with visual separators in the order [Go-to-subtitle + Undo/Redo] [Save] [Time Shift + Apply/Reset] | [Render MP4 / Download] | [Re-transcribe, Replace Video, Upload new], with compact consistent button sizing.
    8. Buttons (D-05): confirm the Upload SRT button in the text editor is a compact inline button consistent with the other controls.
    9. Regression: adjust a global style (e.g. font size) and confirm the preview updates live; confirm the top/bottom resize handle still drags the preview/editor split.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Toolbar markup + CSS reorganization of an existing single-user local tool; no new inputs, network, storage, or dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (RESEARCH confirms zero new dependencies) |
</threat_model>

<verification>
- `cd packages/frontend && npx tsc --noEmit -p tsconfig.json` exits 0 after Tasks 1-2.
- Human-verify checkpoint confirms the full Phase 12 UI (D-01 through D-15) in the running app.
</verification>

<success_criteria>
The toolbar is reorganized into logical groups with visual separators in the D-02 order and compact consistent sizing (D-01, D-02, D-03), Go-to-subtitle leads the first group as a compact button (D-05), and a human confirms the entire Phase 12 restructure works in the running app.
</success_criteria>

<output>
Create `.planning/phases/12-ui-ux-layout-improvements/12-03-SUMMARY.md` when done
</output>
