---
phase: 12-ui-ux-layout-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/GlobalStyleSidePanel.tsx
  - packages/frontend/src/components/GlobalStyleSidePanel.css
  - packages/frontend/src/components/LaneSidePanel.css
  - packages/frontend/src/pages/SubtitlesPage.tsx
  - packages/frontend/src/pages/SubtitlesPage.css
  - packages/frontend/src/components/PhraseStyleSidePanel.tsx
  - packages/frontend/src/components/PhraseStyleSidePanel.css
  - packages/frontend/src/components/StyleDrawer/StyleDrawer.tsx
  - packages/frontend/src/components/StyleDrawer/StyleDrawer.css
autonomous: true
requirements: [D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15]

must_haves:
  truths:
    - Global Styling controls are always visible in a permanent right side panel (no overlay drawer, no toolbar button to open it)
    - Clicking a phrase or speaker swaps the right panel content to override controls; a close/back button returns it to Global Styling
    - Lane panel (left) and Style panel (right) are full-height columns flanking a center column that holds preview + toolbar + editor
    - Lane panel is visible on all three stages (Text Edit, Word Timing, Animation), independent of preview collapse state
    - Both side panels are fixed-width and collapsible via toggle (not drag-resizable); the right panel starts expanded
  artifacts:
    - packages/frontend/src/components/GlobalStyleSidePanel.tsx
    - packages/frontend/src/components/GlobalStyleSidePanel.css
  key_links:
    - SubtitlesPage rightPanelMode state drives GlobalStyleSidePanel content (global | phrase | speaker)
    - Center column carries the topPercent height split and the resize handle
---

<objective>
Convert the SubtitlesPage editing layout from a single vertical column with absolutely-positioned overlay panels into a three-column full-height flex layout: permanent Lane panel (left), a center column holding preview + toolbar + editor, and a permanent Global Style panel (right). The overlay StyleDrawer is removed entirely and its content moves into the always-visible right panel, which defaults to Global Styling and swaps to phrase/speaker override controls on selection.

Purpose: Deliver the core structural decisions of Phase 12 (D-06 through D-15) — a permanent right style panel and full-height flanking side panels.
Output: New GlobalStyleSidePanel component, converted panel CSS, restructured SubtitlesPage layout, and removal of the StyleDrawer overlay and PhraseStyleSidePanel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ui-ux-layout-improvements/12-CONTEXT.md
@.planning/phases/12-ui-ux-layout-improvements/12-RESEARCH.md
@.planning/phases/12-ui-ux-layout-improvements/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols and files created or removed by Phase 12 (exclude these from source-drift verification):

- NEW component `GlobalStyleSidePanel` (function export) in `packages/frontend/src/components/GlobalStyleSidePanel.tsx`
- NEW type `RightPanelMode` relocated to and exported from `GlobalStyleSidePanel.tsx` with variants `{ type: 'global' }`, `{ type: 'phrase'; phraseIndex: number }`, `{ type: 'speaker'; speakerId: string }`
- NEW stylesheet `GlobalStyleSidePanel.css` with BEM prefix `global-style-panel`
- NEW CSS classes (Plan 02): `subtitles-page__toolbar-group`, `subtitles-page__toolbar-separator`
- NEW CSS class `subtitles-page__center` (center-column wrapper)
- REMOVED: `StyleSidePanel` export and `RightPanelMode` type from `PhraseStyleSidePanel.tsx`; files `PhraseStyleSidePanel.tsx`, `PhraseStyleSidePanel.css`
- REMOVED: `StyleDrawer` component, `DrawerMode` type; files `StyleDrawer/StyleDrawer.tsx`, `StyleDrawer/StyleDrawer.css`
- REMOVED: `subtitles-page__styling-btn` toolbar button (Global Styling) and `drawerMode`/`setDrawerMode` state in `SubtitlesPage.tsx`
- KEPT (do not delete): `StyleDrawer/PhraseStylePanel.tsx` (override controls, still consumed by the new panel), `StylePanel/StylePanel.tsx`, `StylePanel/SpeakerStylePanel.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create consolidated GlobalStyleSidePanel and convert LaneSidePanel to flow layout</name>
  <files>
    packages/frontend/src/components/GlobalStyleSidePanel.tsx (new),
    packages/frontend/src/components/GlobalStyleSidePanel.css (new),
    packages/frontend/src/components/LaneSidePanel.css (modify)
  </files>
  <read_first>
    - packages/frontend/src/components/PhraseStyleSidePanel.tsx (current RightPanelMode union + StyleSidePanel mode-swap structure to fold in)
    - packages/frontend/src/components/PhraseStyleSidePanel.css (current right-panel styling to fold into new file)
    - packages/frontend/src/components/StyleDrawer/StyleDrawer.tsx (the StylePanel + SpeakerStylePanel body to migrate into the global branch)
    - packages/frontend/src/components/LaneSidePanel.tsx (collapse toggle pattern: local collapsed state, __toggle button, __toggle-arrow, __content)
    - packages/frontend/src/components/LaneSidePanel.css (BEM structure to mirror; the absolute→flow conversion target)
    - packages/frontend/src/components/StyleDrawer/PhraseStylePanel.tsx (override control component consumed in phrase mode — confirm props: phraseIndex)
    - packages/frontend/src/components/StylePanel/SpeakerStylePanel.tsx (confirm optional singleSpeakerId prop used in speaker mode)
    - .planning/phases/12-ui-ux-layout-improvements/12-RESEARCH.md (GlobalStyleSidePanel target shape and full-height flow CSS)
    - .planning/phases/12-ui-ux-layout-improvements/12-PATTERNS.md (Analog assignments; recommendation to consolidate into one panel)
  </read_first>
  <action>
    Create `GlobalStyleSidePanel.tsx` as the single consolidated right-panel component (per D-09 single-slot). Move the `RightPanelMode` type into this file and export it, extending the existing union with a `{ type: 'global' }` variant so it is: `{ type: 'global' }` | `{ type: 'phrase'; phraseIndex: number }` | `{ type: 'speaker'; speakerId: string }`. Component props: `mode: RightPanelMode`, `onBack: () => void`, `collapsed: boolean`, `onToggleCollapse: () => void`. Derive `isGlobal = mode.type === 'global'` and a `title` of `'Global Styling'` (global), `'Phrase Style'` (phrase), or `'Speaker Style'` (speaker). Render the LaneSidePanel toggle skeleton: a `global-style-panel__toggle` button and a `global-style-panel__content` region. The toggle's onClick is `onToggleCollapse` when `isGlobal` and `onBack` when not global (so the override view's toggle acts as the close/back-to-global control, satisfying D-11). The toggle-arrow shows the collapse chevron when global (◀ expanded / ▶ collapsed) and the multiplication-sign close glyph when in an override mode. In the content region render: `StylePanel` + `SpeakerStylePanel` when global; `PhraseStylePanel` with `phraseIndex={mode.phraseIndex}` when phrase; `SpeakerStylePanel` with `singleSpeakerId={mode.speakerId}` when speaker. Import `StylePanel` from `./StylePanel/StylePanel.tsx`, `SpeakerStylePanel` from `./StylePanel/SpeakerStylePanel.tsx`, `PhraseStylePanel` from `./StyleDrawer/PhraseStylePanel.tsx`, and `./GlobalStyleSidePanel.css`. Use a wrapper div with class `global-style-panel` plus ` global-style-panel--collapsed` when collapsed. Key the content wrapper by mode identity so React remounts override controls on selection change (mirror the existing key pattern from PhraseStyleSidePanel).

    Create `GlobalStyleSidePanel.css` by copying the LaneSidePanel BEM structure (toggle, toggle-arrow, content scroll region, collapsed modifier — vertical writing-mode toggle when collapsed, content hidden when collapsed) with the prefix `global-style-panel`. The base `.global-style-panel` selector uses the full-height flow model: `position: relative; flex-shrink: 0; width: 240px; height: 100%;` (240px is within the D-15 200-250px discretionary range) with `display: flex; flex-direction: column;` and the elevated background/border/radius/overflow tokens used by LaneSidePanel. `.global-style-panel--collapsed { width: 28px; }`. Do NOT use `position: absolute`, `right: 0`, or any slide-in keyframe animation (D-07 removes the overlay). Use existing CSS custom-property tokens (color-bg-elevated, color-border, color-surface-secondary, color-text-secondary, spacing-*, font-size-*, transition-fast) — do not invent token names.

    Convert `LaneSidePanel.css`: change the `.lane-side-panel` base rule from the absolute-overlay model (`position: absolute; left: 0; top: 0; bottom: 0; z-index: 10; width: 200px;`) to the full-height flow model `position: relative; flex-shrink: 0; width: 220px; height: 100%; z-index: auto;` keeping the existing display/flex-direction/background/border/border-radius/overflow/transition lines. In `.lane-side-panel--collapsed` keep `width: 28px` and remove the `bottom: auto` line. Leave every other selector in the file (toggle, content, section-title, speaker-row, checkbox, detect-btn) unchanged.
  </action>
  <verify>
    <automated>cd packages/frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `packages/frontend/src/components/GlobalStyleSidePanel.tsx` exists and exports both `GlobalStyleSidePanel` and the `RightPanelMode` type: `grep -qE "export function GlobalStyleSidePanel" packages/frontend/src/components/GlobalStyleSidePanel.tsx && grep -qE "export type RightPanelMode" packages/frontend/src/components/GlobalStyleSidePanel.tsx`
    - The RightPanelMode union in the new file includes all three variants: `grep -c "type: 'global'" packages/frontend/src/components/GlobalStyleSidePanel.tsx` returns at least 1, and both `type: 'phrase'` and `type: 'speaker'` appear.
    - `GlobalStyleSidePanel.css` contains no absolute positioning or slide-in animation: `grep -vE '^\s*#' packages/frontend/src/components/GlobalStyleSidePanel.css | grep -Ec 'position:\s*absolute|@keyframes'` returns 0.
    - `.lane-side-panel` uses flow layout: `grep -A3 '^.lane-side-panel {' packages/frontend/src/components/LaneSidePanel.css` shows `position: relative` and `height: 100%`, and `bottom: auto` is absent from the collapsed rule.
    - `npx tsc --noEmit -p tsconfig.json` in packages/frontend exits 0.
  </acceptance_criteria>
  <done>New GlobalStyleSidePanel component and stylesheet exist as a valid single-slot right panel; LaneSidePanel.css is flow-based; frontend typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure SubtitlesPage into three-column layout and remove StyleDrawer overlay</name>
  <files>
    packages/frontend/src/pages/SubtitlesPage.tsx (modify),
    packages/frontend/src/pages/SubtitlesPage.css (modify),
    packages/frontend/src/components/PhraseStyleSidePanel.tsx (delete),
    packages/frontend/src/components/PhraseStyleSidePanel.css (delete),
    packages/frontend/src/components/StyleDrawer/StyleDrawer.tsx (delete),
    packages/frontend/src/components/StyleDrawer/StyleDrawer.css (delete)
  </files>
  <read_first>
    - packages/frontend/src/pages/SubtitlesPage.tsx (imports at lines 9,19-20; state at lines 53-54; resize handler at 217-240; transcribed-state JSX at 684-961)
    - packages/frontend/src/pages/SubtitlesPage.css (layout selectors at lines 1-70: --preview, __top, __top-row, __top--collapsed, __top-controls, resize-handle)
    - packages/frontend/src/components/GlobalStyleSidePanel.tsx (the component + RightPanelMode created in Task 1 — import source)
    - .planning/phases/12-ui-ux-layout-improvements/12-RESEARCH.md (Outer layout change target and Pitfalls 1-5)
    - .planning/phases/12-ui-ux-layout-improvements/12-PATTERNS.md (exact SubtitlesPage.tsx line references for edits)
  </read_first>
  <action>
    Update imports (D-06, D-07): remove `import { StyleDrawer, type DrawerMode } from '../components/StyleDrawer/StyleDrawer.tsx'` and `import { StyleSidePanel, type RightPanelMode } from '../components/PhraseStyleSidePanel.tsx'`; add `import { GlobalStyleSidePanel, type RightPanelMode } from '../components/GlobalStyleSidePanel.tsx'`.

    Update state: remove the `drawerMode`/`setDrawerMode` state line entirely (D-07/D-08). Change `rightPanelMode` initialization from `useState<RightPanelMode | null>(null)` to `useState<RightPanelMode>({ type: 'global' })` (D-09, Pitfall 4). Add a new local collapse state for the right panel, e.g. `rightPanelCollapsed` initialized to `false` (D-06 starts expanded, D-15 still collapsible).

    Restructure the transcribed-state JSX (D-13, D-14): the outer `div.subtitles-page.subtitles-page--preview` (keep `ref={containerRef}`) becomes a three-column flex row. Its direct children in order are: (1) `<LaneSidePanel .../>` moved OUT of the `!previewCollapsed` gate and OUT of `subtitles-page__top-row` so it is a permanent outer-level sibling visible on all stages (D-14, Pitfall 3); (2) a new `<div className="subtitles-page__center">` wrapper containing everything currently inside `--preview` (the `subtitles-page__top` block, the resize handle, and the `subtitles-page__bottom` block); (3) `<GlobalStyleSidePanel mode={rightPanelMode} onBack={() => setRightPanelMode({ type: 'global' })} collapsed={rightPanelCollapsed} onToggleCollapse={() => setRightPanelCollapsed((c) => !c)} />` as a permanent outer-level sibling (D-06, D-09). Inside `subtitles-page__top-row`, `PreviewPanel` becomes the sole child (remove the LaneSidePanel and the old `StyleSidePanel` render at lines 713-718). Remove the `<StyleDrawer .../>` element (line 959). Keep `<AutoSaveIndicator/>` inside the outer div.

    Remove the toolbar "Global Styling" button (D-08): delete the `subtitles-page__styling-btn` button block (lines 818-824) and its `setDrawerMode` handler. Do NOT reorganize the remaining toolbar buttons — that is Plan 02's scope.

    Keep all existing `setRightPanelMode({ type: 'speaker'/'phrase', ... })` edit-trigger call sites unchanged (MiniTimeline onEditSpeaker, TextEditor onEditPhrase, TimingEditor onEditSpeaker/onEditPhrase) — the extended union still accepts these (D-10). Do not add any click-away/auto-close handler that resets to global — only the panel's own back button returns to global (D-12).

    Resize handler / containerRef (Pitfall 2): keep `containerRef` on the outer `--preview` div. Because side panels are full-height siblings in the row, the outer div height equals the center column height, so the existing `getBoundingClientRect().height` percentage math stays correct. Do not move containerRef onto a side panel.

    Update `SubtitlesPage.css` (D-13): change `.subtitles-page--preview` from `flex-direction: column` to `flex-direction: row` (keep width/margin/padding/height/overflow). Add `.subtitles-page__center { flex: 1; min-width: 0; display: flex; flex-direction: column; overflow: hidden; }` — this wrapper carries the vertical top/bottom split. The `.subtitles-page__top-row` `position: relative` + `justify-content: center` centering is no longer needed for panel overlay; keep it centering the sole PreviewPanel child or simplify to a plain flex centering row (do not leave rules that assume absolutely-positioned panel children). Leave `.subtitles-page__top`, `.subtitles-page__top--collapsed`, `.subtitles-page__top-controls`, and resize-handle rules otherwise intact.

    Delete the four superseded files: `PhraseStyleSidePanel.tsx`, `PhraseStyleSidePanel.css`, `StyleDrawer/StyleDrawer.tsx`, `StyleDrawer/StyleDrawer.css` (D-07). Do NOT delete `StyleDrawer/PhraseStylePanel.tsx` (still consumed by GlobalStyleSidePanel).
  </action>
  <verify>
    <automated>cd packages/frontend && npx tsc --noEmit -p tsconfig.json && test ! -e src/components/StyleDrawer/StyleDrawer.tsx && test ! -e src/components/PhraseStyleSidePanel.tsx && test -e src/components/StyleDrawer/PhraseStylePanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No remaining references to StyleDrawer or drawerMode in the page: `grep -Ec 'StyleDrawer|drawerMode|subtitles-page__styling-btn' packages/frontend/src/pages/SubtitlesPage.tsx` returns 0.
    - SubtitlesPage imports the new panel: `grep -q "GlobalStyleSidePanel" packages/frontend/src/pages/SubtitlesPage.tsx`.
    - rightPanelMode initializes to global (not null): `grep -qE "useState<RightPanelMode>\(\{ type: 'global' \}\)" packages/frontend/src/pages/SubtitlesPage.tsx`.
    - The center-column wrapper exists in markup and CSS: `grep -q "subtitles-page__center" packages/frontend/src/pages/SubtitlesPage.tsx` and `grep -q "subtitles-page__center" packages/frontend/src/pages/SubtitlesPage.css`.
    - `.subtitles-page--preview` is a row: `grep -A3 '.subtitles-page--preview {' packages/frontend/src/pages/SubtitlesPage.css` shows `flex-direction: row`.
    - Deleted files absent, kept file present (asserted by the verify command).
    - Frontend typecheck exits 0.
  </acceptance_criteria>
  <done>SubtitlesPage renders a three-column full-height layout with permanent lane + style panels; StyleDrawer overlay and PhraseStyleSidePanel are removed; the "Global Styling" toolbar button is gone; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side React/CSS layout refactor of an existing single-user local tool; introduces no new inputs, network calls, storage, or dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Right panel now always renders style/override controls | low | accept | Same data already rendered in the previous overlay drawer; no new data surfaced; single-user local app |
| T-12-02 | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (RESEARCH confirms zero new dependencies) |
</threat_model>

<verification>
- `cd packages/frontend && npx tsc --noEmit -p tsconfig.json` exits 0 after both tasks.
- Manual (covered by the phase human-verify checkpoint in Plan 02): app launched via `just dev`, transcribed/loaded project shows lane panel left + style panel right as full-height columns; Global Styling controls visible by default; clicking a phrase/speaker swaps right panel to override controls; back button returns to Global Styling; clicking elsewhere does not auto-return; lane panel visible on Text Edit, Word Timing, and Animation stages.
</verification>

<success_criteria>
Global Styling is a permanent, always-visible right panel (D-06) with the overlay drawer and its toolbar button removed (D-07, D-08); the right panel is a single slot defaulting to global and swapping to phrase/speaker overrides on selection with a back-to-global close control and no click-away auto-return (D-09, D-10, D-11, D-12); lane and style panels are full-height fixed-width collapsible columns flanking a center column, with the lane panel visible on all stages (D-13, D-14, D-15).
</success_criteria>

<output>
Create `.planning/phases/12-ui-ux-layout-improvements/12-01-SUMMARY.md` when done
</output>
