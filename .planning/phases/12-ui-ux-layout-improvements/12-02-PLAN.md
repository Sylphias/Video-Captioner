---
phase: 12-ui-ux-layout-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/StageTabBar.tsx
  - packages/frontend/src/components/StageTabBar.css
  - packages/frontend/src/components/TextEditor/TextEditor.css
autonomous: true
requirements: [D-04, D-05]

must_haves:
  truths:
    - Stage tabs (Text Edit, Word Timing, Animation) read as proper tabs — active tab clearly highlighted, inactive tabs muted, consistent sizing, matching the dark theme
    - The Upload SRT button is a compact inline button visually consistent with the app's other compact bordered controls
  artifacts:
    - packages/frontend/src/components/StageTabBar.css
    - packages/frontend/src/components/TextEditor/TextEditor.css
  key_links:
    - StageTabBar cosmetic classes drive tab active/inactive appearance
    - srt-import-btn shares the compact bordered-button visual language of subtitles-page__goto-btn
---

<objective>
Polish the stage tab bar so the three stages read as proper tabs (active highlighted, inactive muted, consistent sizing) and resize the Upload SRT button into a compact inline button consistent with the app's other bordered controls. Cosmetic-only: no layout restructure, no logic changes.

Purpose: Deliver D-04 (stage tab styling) and the Upload-SRT portion of D-05 (compact inline text-editor button).
Output: Updated StageTabBar labels/CSS and TextEditor SRT button CSS. This plan touches no files shared with the layout (12-01) or toolbar (12-03) plans, so it runs in parallel in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ui-ux-layout-improvements/12-CONTEXT.md
@.planning/phases/12-ui-ux-layout-improvements/12-RESEARCH.md
@.planning/phases/12-ui-ux-layout-improvements/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan adds no new symbols or files — it edits existing components/CSS only. (See 12-01-PLAN.md `<artifacts_this_phase_produces>` for the phase-wide list of created/removed symbols.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Polish StageTabBar into proper tabs</name>
  <files>
    packages/frontend/src/components/StageTabBar.tsx (modify),
    packages/frontend/src/components/StageTabBar.css (modify)
  </files>
  <read_first>
    - packages/frontend/src/components/StageTabBar.tsx (STAGES label array; tab render with --active/--suggested classes; no logic changes intended)
    - packages/frontend/src/components/StageTabBar.css (current tab, --active, --suggested, hover, dot rules)
    - packages/frontend/src/index.css or tokens stylesheet (confirm token names: color-accent-green, color-text-primary, color-text-secondary, color-bg-base, color-border, spacing-*, font-size-*, transition-fast) before using them
    - .planning/phases/12-ui-ux-layout-improvements/12-CONTEXT.md (D-04)
  </read_first>
  <action>
    Shorten the `text` stage label from `'Text Edit View'` to `'Text Edit'` in the `STAGES` array in `StageTabBar.tsx` so all three labels are consistent-length compact tab titles (`Text Edit`, `Word Timing`, `Animation`). Make no other component/logic changes — active/suggested/hover behavior already works.

    In `StageTabBar.css`, tune the existing selectors for the D-04 "proper tabs" outcome (all within Claude's discretion per CONTEXT.md): increase the tab horizontal padding for a more deliberate consistent-sizing feel (e.g. bump `.stage-tab-bar__tab` padding to `var(--spacing-sm) var(--spacing-lg)` if those tokens exist, otherwise keep the sm/md tokens), ensure inactive tabs are visibly muted versus active by lowering the inactive `color` toward `--color-text-secondary` while keeping the active tab at `--color-text-primary` with the existing green bottom-border and `font-weight` (raise active weight to 600 if 500 reads too subtle), and keep the sticky bar background at `--color-bg-base`. Do not remove the bottom-border underline indicator or the suggested-dot rule; do not switch to a boxed/pill tab style — retain the underline-tab pattern. Use only existing CSS tokens.
  </action>
  <verify>
    <automated>cd packages/frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - The `text` stage label is exactly `Text Edit` (no "View"): `grep -q "label: 'Text Edit'" packages/frontend/src/components/StageTabBar.tsx` and `grep -c "Text Edit View" packages/frontend/src/components/StageTabBar.tsx` returns 0.
    - Active-tab distinction preserved: `grep -q "stage-tab-bar__tab--active" packages/frontend/src/components/StageTabBar.css` and the active rule still sets `border-bottom-color: var(--color-accent-green)`.
    - Underline pattern retained (no pill/box conversion): the `.stage-tab-bar__tab` base still declares `border-bottom: 2px solid transparent`.
    - Frontend typecheck exits 0.
  </acceptance_criteria>
  <done>Stage tab labels are compact and consistent; active tab is clearly highlighted and inactive tabs muted using the existing underline-tab pattern; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Make the Upload SRT button a compact consistent inline button</name>
  <files>
    packages/frontend/src/components/TextEditor/TextEditor.css (modify)
  </files>
  <read_first>
    - packages/frontend/src/components/TextEditor/TextEditor.css (current `.srt-import-btn` and `:hover` rules at ~lines 405-425)
    - packages/frontend/src/components/TextEditor/TextEditor.tsx (line ~635 confirms the button uses className `srt-import-btn`; no markup change needed)
    - packages/frontend/src/pages/SubtitlesPage.css (the `.subtitles-page__goto-btn` compact bordered-button reference at lines 264-278 — the visual target to match)
    - .planning/phases/12-ui-ux-layout-improvements/12-CONTEXT.md (D-05)
  </read_first>
  <action>
    Update `.srt-import-btn` in `TextEditor.css` to match the compact bordered-button visual language of `.subtitles-page__goto-btn` (D-05 "styled consistently with other controls"). Change the border from `1px dashed var(--color-border)` to `1px solid var(--color-border)` so it reads as a real compact control rather than a placeholder dropzone; remove the fixed `height: 32px` so height is driven by the shared `var(--spacing-xs) var(--spacing-md)` padding like goto-btn; keep `border-radius: 4px`, the transparent background, `color: var(--color-text-secondary)`, `font-size: var(--font-size-sm)`, and the hover treatment (border-color → accent-green, color → text-primary). Retain the positional `align-self: flex-start` and the `margin-left`/`margin-bottom` indentation so the button stays aligned in the text-editor column (placement unchanged per D-05 "stay in the text editor area"). Do not edit `TextEditor.tsx` — className is already correct.
  </action>
  <verify>
    <automated>cd packages/frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - The SRT button uses a solid border and no fixed pixel height: `grep -A12 '.srt-import-btn {' packages/frontend/src/components/TextEditor/TextEditor.css` shows `border: 1px solid var(--color-border)` and contains no `height: 32px` line.
    - Compact padding retained: the same rule still declares `padding: var(--spacing-xs) var(--spacing-md)`.
    - Hover consistency retained: `grep -A3 '.srt-import-btn:hover {' packages/frontend/src/components/TextEditor/TextEditor.css` shows `border-color: var(--color-accent-green)`.
    - No markup change: `git diff --name-only` does not include `TextEditor.tsx`.
    - Frontend typecheck exits 0.
  </acceptance_criteria>
  <done>Upload SRT is a compact solid-bordered inline button visually consistent with goto-btn, positioned unchanged in the text editor; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Cosmetic CSS/label-only changes to an existing single-user local tool; no new inputs, network, storage, or dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (RESEARCH confirms zero new dependencies) |
</threat_model>

<verification>
- `cd packages/frontend && npx tsc --noEmit -p tsconfig.json` exits 0.
- Manual (covered by the phase human-verify checkpoint in Plan 03): stage tabs read as proper tabs with an obviously-active tab and muted inactive tabs; the Upload SRT button looks like a compact bordered control consistent with Go-to-subtitle.
</verification>

<success_criteria>
Stage tabs are properly styled with a highlighted active tab, muted inactive tabs, and consistent compact sizing (D-04); the Upload SRT button is a compact inline button consistent with the app's other controls, unchanged in placement (D-05).
</success_criteria>

<output>
Create `.planning/phases/12-ui-ux-layout-improvements/12-02-SUMMARY.md` when done
</output>
