---
phase: 11
slug: text-editor-enhancements
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-28
---

# Phase 11 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom CSS token system (tokens.css) |
| Preset | not applicable |
| Component library | none — hand-rolled components with BEM-style CSS |
| Icon library | none — text labels and Unicode glyphs used for icons |
| Font | -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif (var(--font-family)) |

Source: `packages/frontend/src/styles/tokens.css` (read directly from repo)

---

## Spacing Scale

All values from `tokens.css`. Phase 11 adds no new spacing tokens.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (--spacing-xs) | Checkbox gap, inline word padding, icon-to-label gaps |
| sm | 8px (--spacing-sm) | Row gap within toolbar, find/replace input padding |
| md | 16px (--spacing-md) | TextEditor outer padding, toolbar horizontal padding |
| lg | 24px (--spacing-lg) | Section separation within find/replace preview modal |
| xl | 32px (--spacing-xl) | TextEditor bottom padding (existing), modal max-height margin |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Phrase row checkbox hit area: 24x24px (minimum touch target while keeping visual size 16px)
- Checkbox column gutter: 20px wide (flex-shrink: 0, between line number and speaker dot)
- BulkActionsToolbar height: 36px fixed — justified exception: matches the density of the existing TextEditor header bar (--header-height: 48px scale); 32px would feel cramped next to the adjacent header; this is an inner-chrome toolbar, not a standalone button row

---

## Typography

All sizes derived from existing tokens. Phase 11 introduces no new size tokens.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body (phrase text, editable) | 16px (--font-size-base, 1rem) | 400 (regular) | 1.6 |
| Label (toolbar buttons, input labels, count badges) | 14px (--font-size-sm, 0.875rem) | 400 (regular) | 1.4 |
| Heading (find/replace preview modal title, bulk actions count) | 18px (--font-size-lg, 1.125rem) | 600 (semibold) | 1.2 |
| Metadata (line numbers, confidence score tooltip, match count) | 14px (--font-size-sm, 0.875rem) | 400 (regular) | 1.4 |

Source: `tokens.css` — `--font-size-sm`, `--font-size-base`, `--font-size-lg` pre-existing.
Only 2 weights used: 400 (regular) for all body/label text, 600 (semibold) for modal heading.

---

## Color

All values from `tokens.css`. No new color tokens introduced.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #1a1a1a (--color-bg-base) | Page background, TextEditor scroll area background |
| Secondary (30%) | #242424 / #2e2e2e (--color-bg-surface / --color-bg-elevated) | Find/replace bar background (#2e2e2e), BulkActionsToolbar background (#2e2e2e), selected row tint base, preview modal background (#242424) |
| Accent (10%) | #4caf72 (--color-accent-green) | See reserved list below |
| Destructive | #e05454 (--color-error) | Delete button in BulkActionsToolbar only |

Accent reserved for:
1. Find/replace input focus border (`border-color: var(--color-accent-green)`)
2. Find match highlight within phrase text (background tint: `color-mix(in srgb, var(--color-accent-green-muted) 60%, transparent)`)
3. Checkbox `accent-color` on phrase row checkboxes
4. Selected phrase row background tint (`color-mix(in srgb, var(--color-accent-green-muted) 50%, transparent)`)
5. Line number hover color (existing, unchanged)
6. Active phrase text color (existing, unchanged)
7. "Replace All" confirm button (primary CTA in preview modal)

Warning color (#d4a047 --color-warning) reserved for:
- Low-confidence word underline decoration (dotted underline on words with confidence < 0.7)

Source: RESEARCH.md CSS examples + tokens.css

---

## Component Inventory

Three new components introduced. All others are modifications to existing TextEditor.tsx / TextEditor.css.

### FindReplaceBar (new — `FindReplaceBar.tsx` + `FindReplaceBar.css`)

**Screen focal point:** The Find input (`<input type="text" placeholder="Find…">`) receives `autoFocus` when the bar opens. It is the primary interaction target for this screen state. No other element competes for initial focus.

**Visual anatomy:**
- Container: `position: sticky; top: 0; z-index: 10` within TextEditor scroll area
- Background: `--color-bg-elevated` (#2e2e2e), `border-bottom: 1px solid var(--color-border)`
- Height: 40px (auto from flex alignment with 8px padding top/bottom)
- Layout: `display: flex; align-items: center; gap: var(--spacing-sm); padding: var(--spacing-sm) var(--spacing-md)`

**Elements (left to right):**
1. "Find:" label — 14px, `--color-text-secondary`, width 32px
2. Find input — `background: --color-bg-surface; border: 1px solid --color-border; border-radius: 4px; height: 32px; padding: 0 8px; font-size: 14px; min-width: 160px` — focus state: `border-color: --color-accent-green`
3. "Replace:" label — 14px, `--color-text-secondary`, width 56px
4. Replace input — same style as Find input (height: 32px)
5. Match count badge — "N matches" or "No matches" — 14px, `--color-text-secondary`; turns `--color-error` when 0 matches and find term is non-empty
6. "Replace All" button — 14px, background `--color-accent-green`, color white, border-radius 4px, padding 4px 8px, height 32px — disabled (opacity 0.4) when 0 matches
7. Close (×) button — 18px, `--color-text-secondary`, no border, background transparent, hover color `--color-text-primary`; `aria-label="Close find and replace"`

**States:**
- Hidden by default; toggled by Ctrl+H
- Enters via slide-down transition: `max-height 150ms ease, opacity 150ms ease` (max-height: 0 → 48px)
- Exits via slide-up reverse

### BulkActionsToolbar (new — `BulkActionsToolbar.tsx` + `BulkActionsToolbar.css`)

**Visual anatomy:**
- Renders above the phrase list, outside the scroll area (inside TextEditor container but above the scrollable `.text-editor__lines` wrapper)
- Background: `--color-bg-elevated` (#2e2e2e)
- Border: `1px solid --color-border`; `border-radius: 4px`
- Height: 36px (justified exception — see Spacing Scale); padding: `4px var(--spacing-md)`
- Layout: `display: flex; align-items: center; gap: var(--spacing-sm)`

**Elements (left to right):**
1. Selection count label — "{N} selected" — 14px, `--color-text-secondary`
2. Separator: `1px solid --color-border`, height 20px
3. "Merge Phrases" button — 14px, background transparent, border `1px solid --color-border`, border-radius 4px, height 32px, padding 0 8px; hover: `border-color: --color-accent-green; color: --color-text-primary`
4. "Delete Phrases" button — same style as Merge Phrases but hover: `border-color: --color-error; color: --color-error`
5. "Reassign Speaker" dropdown button — 14px, same base style as Merge Phrases; opens inline dropdown list of speaker names; chevron icon (▾ Unicode U+25BE, 12px)
6. Keyboard hint — "Ctrl+M merge · Del delete" — 14px, `--color-text-disabled`, positioned `margin-left: auto`

**States:**
- Hidden when selectedPhraseIndices.size < 2
- Appears via `opacity 0 → 1` transition over 100ms ease; does NOT slide or animate height
- Disappears immediately when selection drops below 2 (no exit transition to stay snappy)

### Phrase Row — Selection Mode Additions (modifications to TextEditor.tsx)

When any phrase is selected OR when pointer enters the text editor (hover intent to select):

**Checkbox column:**
- Position: between line number gutter and speaker dot
- Width: 20px; flex-shrink: 0
- Checkbox element: 16x16px native `<input type="checkbox">` with `accent-color: var(--color-accent-green)`
- Visible: always when 1+ phrases are selected; opacity 0 (but space reserved) otherwise to avoid layout shift
- Alternative: Shift+click on the row itself also selects without requiring the checkbox

**Selected row state:**
- Background: `color-mix(in srgb, var(--color-accent-green-muted) 50%, transparent)` on `.text-editor__line--selected`
- Border-radius: 4px (matches existing hover style)
- No color change on the text content itself — text remains `--color-text-primary`

### Low-Confidence Word Underline (modification to phrase contentEditable rendering)

- CSS class: `.text-editor__word--low-confidence`
- Style: `text-decoration: underline; text-decoration-style: dotted; text-decoration-color: var(--color-warning)` (#d4a047)
- Cursor: `help` on hover
- Threshold constant: `CONFIDENCE_THRESHOLD = 0.7` in TextEditor.tsx (TypeScript const, not a UI control)
- Tooltip: native HTML `title` attribute on the `<span>` — content: `"Confidence: {N}%"` (e.g. "Confidence: 62%")

### Replace All Preview Modal

**Visual anatomy:**
- Overlay: `position: fixed; inset: 0; background: rgba(0,0,0,0.6); z-index: 100`
- Modal container: `position: fixed; top: 50%; left: 50%; transform: translate(-50%,-50%); background: --color-bg-surface; border: 1px solid --color-border; border-radius: 8px; width: 520px; max-height: 70vh; display: flex; flex-direction: column`
- Header bar: `padding: var(--spacing-md); border-bottom: 1px solid --color-border` — title "Replace All Preview" in 18px semibold + close × button right-aligned
- Body (scrollable): `padding: var(--spacing-md); overflow-y: auto; flex: 1` — list of match rows
- Footer: `padding: var(--spacing-sm) var(--spacing-md); border-top: 1px solid --color-border; display: flex; justify-content: flex-end; gap: var(--spacing-sm)` — "Dismiss Preview" button + "Confirm Replace All" button

**Match row anatomy:**
- Line number: 14px `--color-text-disabled`, tabular-nums, width 32px
- Before text: 14px `--color-text-secondary`, with find term highlighted in `color-mix(in srgb, var(--color-warning) 30%, transparent)` background
- Arrow: → (U+2192), 14px `--color-text-disabled`
- After text: 14px `--color-text-primary`, with replace term highlighted in `color-mix(in srgb, var(--color-accent-green-muted) 60%, transparent)` background
- Warning note (bottom of body, if any): "Note: Replacing text may redistribute word timestamps if word count changes." — 14px `--color-text-secondary`, italic

---

## Interaction Contract

### Find/Replace Bar

| Trigger | Result |
|---------|--------|
| Ctrl+H | Toggle FindReplaceBar open/closed |
| Escape (while bar focused) | Close FindReplaceBar |
| Type in Find input | Match count updates live after 300ms debounce |
| Type in Replace input | No effect until "Replace All" triggered |
| Click "Replace All" | Open preview modal if matches > 0 |
| Click "Confirm Replace All" in modal | Execute replacements, close modal, close bar, show toast "Replaced N occurrences" |
| Click "Dismiss Preview" in modal | Close modal only, bar remains open |

### Multi-Select

| Trigger | Result |
|---------|--------|
| Click checkbox on row | Toggle that phrase in selection |
| Shift+click a row | Range-select from last clicked index to clicked index |
| Ctrl+A (not in contentEditable) | Select all phrases |
| Escape | Clear selection |
| Click empty area in text editor (not a row) | Clear selection |

### Keyboard Shortcuts (TextEditor container scope only)

| Shortcut | Condition | Result |
|----------|-----------|--------|
| Ctrl+H | always | Toggle find/replace bar |
| Ctrl+A | not in contentEditable | Select all phrases |
| Delete or Backspace | 1+ selected, not in contentEditable | Delete selected phrases (with confirmation if > 3 phrases) |
| Ctrl+M | 2+ selected, not in contentEditable | Merge selected phrases, clear selection |
| Ctrl+D | 1 selected, not in contentEditable | Duplicate that phrase, clear selection |
| Ctrl+Shift+Up | 1+ selected, not in contentEditable | Move selected phrases up one position |
| Ctrl+Shift+Down | 1+ selected, not in contentEditable | Move selected phrases down one position |
| Tab | in contentEditable | Move focus to next phrase contentEditable |
| Shift+Tab | in contentEditable | Move focus to previous phrase contentEditable |
| Tab | not in contentEditable | Move focus to next phrase contentEditable |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (find/replace) | "Replace All" |
| Find/replace bar — match count (matches) | "{N} match" / "{N} matches" |
| Find/replace bar — match count (none) | "No matches" |
| Find/replace bar — empty find input | "" (placeholder: "Find…") |
| Replace input placeholder | "Replace with…" |
| Preview modal title | "Replace All Preview" |
| Preview modal — note on timestamp redistribution | "Note: Replacing text may redistribute word timestamps if word count changes." |
| Preview modal — confirm CTA | "Confirm Replace All" |
| Preview modal — dismiss | "Dismiss Preview" |
| BulkActionsToolbar — selection count | "{N} selected" |
| BulkActionsToolbar — merge button | "Merge Phrases" |
| BulkActionsToolbar — delete button | "Delete Phrases" |
| BulkActionsToolbar — reassign button | "Reassign Speaker" |
| BulkActionsToolbar — keyboard hint | "Ctrl+M merge  ·  Del delete" |
| Toast — replace success | "Replaced {N} occurrence{s}" |
| Toast — merge success | "Merged {N} phrases" |
| Toast — delete confirmation dialog title (> 3 phrases) | "Delete {N} phrases?" |
| Toast — delete confirmation dialog body | "This cannot be undone in a single step. Use Ctrl+Z to restore." |
| Empty state (no phrases yet) | "No transcript yet. Transcribe a video to begin editing." (existing, unchanged) |
| Low-confidence word tooltip | "Confidence: {N}%" (e.g. "Confidence: 62%") |
| Error (replace with empty find term) | (disable Replace All button — no error message needed; button is greyed) |

Destructive actions in this phase:
1. **Delete selected phrases** — if <= 3 phrases: execute immediately, single undo snapshot; if > 3 phrases: show inline confirmation in BulkActionsToolbar ("Delete 7 phrases? Confirm / Cancel"). No modal for small deletes to keep flow fast.
2. **Replace All** — preview modal required before applying (D-06, locked decision). Not reversible after confirmation except via Ctrl+Z.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none — no external component registries used in this phase | not applicable |

No shadcn, no third-party UI registries. All components are hand-written in the project's existing CSS token + BEM pattern.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
