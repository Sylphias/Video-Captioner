---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/frontend/vite.config.ts
  - packages/frontend/index.html
  - packages/frontend/src/main.tsx
  - packages/frontend/src/App.tsx
  - packages/frontend/src/styles/tokens.css
  - packages/frontend/src/styles/global.css
  - packages/frontend/src/components/Header.tsx
  - packages/frontend/src/components/TabNav.tsx
  - packages/frontend/src/pages/SubtitlesPage.tsx
autonomous: true

must_haves:
  truths:
    - "User can open the app in a browser and see the dark-themed tool shell with header"
    - "Header shows 'Eigen Video Editor' on the left and 'Subtitles' tab on the right"
    - "Subtitles tab is active by default and shows the SubtitlesPage content"
    - "Dark mode is applied — dark background, light text, green accents"
    - "Vite dev server is accessible on the local network (host: true)"
  artifacts:
    - path: "packages/frontend/vite.config.ts"
      provides: "Vite config with React plugin, host: true, proxy to backend"
    - path: "packages/frontend/index.html"
      provides: "HTML entry point"
      contains: "root"
    - path: "packages/frontend/src/App.tsx"
      provides: "App shell with header and page routing"
      min_lines: 15
    - path: "packages/frontend/src/styles/tokens.css"
      provides: "CSS custom properties for dark theme"
      contains: "--color-bg-base"
    - path: "packages/frontend/src/components/Header.tsx"
      provides: "Header with app name and tab navigation"
      min_lines: 10
    - path: "packages/frontend/src/pages/SubtitlesPage.tsx"
      provides: "Subtitles tool page — upload prompt placeholder for now"
      min_lines: 5
  key_links:
    - from: "packages/frontend/src/main.tsx"
      to: "packages/frontend/src/App.tsx"
      via: "React root render"
      pattern: "createRoot.*App"
    - from: "packages/frontend/src/App.tsx"
      to: "packages/frontend/src/components/Header.tsx"
      via: "Component import and render"
      pattern: "import.*Header"
    - from: "packages/frontend/src/styles/tokens.css"
      to: "packages/frontend/src/styles/global.css"
      via: "CSS import chain"
      pattern: "import.*tokens"
---

<objective>
Build the React + Vite frontend shell with dark theme (DaVinci Resolve aesthetic), Header component with app name and tab navigation, and the SubtitlesPage layout. No upload functionality yet — just the visual shell.

Purpose: Establish the frontend application structure, visual identity, and navigation framework. The upload UI will be wired in Plan 04 once the backend upload endpoint exists.
Output: A Vite dev server serving the dark-themed app shell with header, Subtitles tab, and placeholder content area.
</objective>

<execution_context>
@/Users/eigenair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eigenair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vite config, HTML entry point, dark theme tokens, and global styles</name>
  <files>
    packages/frontend/vite.config.ts
    packages/frontend/index.html
    packages/frontend/src/main.tsx
    packages/frontend/src/styles/tokens.css
    packages/frontend/src/styles/global.css
  </files>
  <action>
    **`packages/frontend/vite.config.ts`:**
    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'

    export default defineConfig({
      plugins: [react()],
      server: {
        host: true,    // 0.0.0.0 — LAN accessible (PLAT-02 requirement)
        port: 5173,
        proxy: {
          '/api': {
            target: 'http://localhost:3001',
            changeOrigin: true,
          }
        }
      }
    })
    ```

    **`packages/frontend/index.html`:**
    Standard Vite HTML entry. Set `<html lang="en">` with no class (dark mode is the ONLY mode, applied via global styles). Title: "Eigen Video Editor". Link to `/src/main.tsx` as module script.

    **`packages/frontend/src/main.tsx`:**
    Import `./styles/tokens.css` and `./styles/global.css` (tokens first). Render `<App />` into `#root` using `createRoot`.

    **`packages/frontend/src/styles/tokens.css`:**
    Define CSS custom properties on `:root` per the research-recommended palette (DaVinci Resolve reference, user decision: dark mode with grey + green accents):

    ```css
    :root {
      /* Background layers (darkest to lightest) */
      --color-bg-base:       #1a1a1a;
      --color-bg-surface:    #242424;
      --color-bg-elevated:   #2e2e2e;
      --color-bg-hover:      #383838;

      /* Borders */
      --color-border:        #3a3a3a;
      --color-border-focus:  #4a4a4a;

      /* Text */
      --color-text-primary:  #e8e8e8;
      --color-text-secondary:#a0a0a0;
      --color-text-disabled: #585858;

      /* Green accents — subtle, for interactive elements and highlights */
      --color-accent-green:  #4caf72;
      --color-accent-green-dim: #2d7a4f;
      --color-accent-green-muted: #1e3d2a;

      /* Semantic */
      --color-error:         #e05454;
      --color-warning:       #d4a047;
      --color-success:       var(--color-accent-green);

      /* Upload zone */
      --color-dropzone-border:  #4a4a4a;
      --color-dropzone-active:  var(--color-accent-green);
      --color-dropzone-bg:      var(--color-bg-surface);

      /* Typography */
      --font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      --font-size-sm: 0.875rem;
      --font-size-base: 1rem;
      --font-size-lg: 1.125rem;
      --font-size-xl: 1.25rem;

      /* Spacing */
      --header-height: 48px;
      --spacing-xs: 4px;
      --spacing-sm: 8px;
      --spacing-md: 16px;
      --spacing-lg: 24px;
      --spacing-xl: 32px;

      /* Transitions */
      --transition-fast: 150ms ease;
    }
    ```

    **`packages/frontend/src/styles/global.css`:**
    Reset + base styles:
    - `*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }`
    - `html, body { height: 100%; }`
    - `body { background-color: var(--color-bg-base); color: var(--color-text-primary); font-family: var(--font-family); font-size: var(--font-size-base); line-height: 1.5; -webkit-font-smoothing: antialiased; }`
    - `#root { height: 100%; display: flex; flex-direction: column; }`
    - Style scrollbars for dark mode (thin, dark track, grey thumb)
    - No bright-colored focus rings — use `outline: 1px solid var(--color-border-focus)` on `:focus-visible`
  </action>
  <verify>
    Run `cd packages/frontend && npx vite --host` — Vite dev server should start and display the local + network URLs.
    Open in browser — should show a blank dark page (no components yet, just dark background).
    Stop the dev server.
  </verify>
  <done>
    Vite dev server starts with host: true (LAN accessible) and API proxy to localhost:3001. Dark theme tokens are defined as CSS custom properties. Global styles apply dark background, light text, and system font.
  </done>
</task>

<task type="auto">
  <name>Task 2: Header, TabNav, App shell, and SubtitlesPage</name>
  <files>
    packages/frontend/src/App.tsx
    packages/frontend/src/components/Header.tsx
    packages/frontend/src/components/TabNav.tsx
    packages/frontend/src/pages/SubtitlesPage.tsx
  </files>
  <action>
    **`packages/frontend/src/components/TabNav.tsx`:**
    A component that renders horizontal tabs. Props: `tabs: { id: string, label: string }[]`, `activeTab: string`, `onTabChange: (id: string) => void`.
    - Each tab is a `<button>` (not `<a>` — no page reload per success criteria #2)
    - Active tab has green accent underline/border-bottom (`var(--color-accent-green)`) and primary text color
    - Inactive tabs have secondary text color with hover to primary
    - CSS: flex row, gap between tabs, no border on buttons (reset), cursor pointer
    - Keep it simple — no animations per professional aesthetic

    **`packages/frontend/src/components/Header.tsx`:**
    Fixed header bar at the top of the viewport.
    - Background: `var(--color-bg-elevated)` with a subtle bottom border (`var(--color-border)`)
    - Height: `var(--header-height)` (48px)
    - Layout: flexbox, space-between
    - Left side: "Eigen Video Editor" — `var(--font-size-lg)`, `var(--color-text-primary)`, font-weight 600
    - Right side: `<TabNav>` with the current tabs
    - Horizontal padding: `var(--spacing-lg)` (24px)

    **`packages/frontend/src/pages/SubtitlesPage.tsx`:**
    The landing page for the Subtitles tool. For now (before Plan 04 adds the upload zone), render a centered placeholder:
    - Full remaining viewport height (flex: 1)
    - Centered content (flex, align-items: center, justify-content: center)
    - Placeholder text: "Drop a video or click to upload" in secondary text color
    - This placeholder will be replaced by the actual UploadZone component in Plan 04

    **`packages/frontend/src/App.tsx`:**
    App shell component:
    - State: `activeTab` (string, default: `'subtitles'`)
    - Define tabs array: `[{ id: 'subtitles', label: 'Subtitles' }]` — single tab only per user decision (no placeholder/coming-soon slots)
    - Render `<Header>` with TabNav passing tabs, activeTab, onTabChange
    - Below header: render page based on activeTab — for now only SubtitlesPage
    - Use a `<main>` element that takes remaining viewport height (flex: 1)
    - The header is fixed/sticky at top; main content scrolls if needed

    Style all components using CSS modules or inline styles referencing CSS custom properties. Prefer co-located `.css` files (e.g., `Header.css` alongside `Header.tsx`) or a single `components.css` — whichever keeps the code cleanest. Do NOT use Tailwind (not in the stack). Do NOT use styled-components.

    All colors MUST reference CSS custom properties from tokens.css — no hard-coded hex values in components.
  </action>
  <verify>
    Run `cd packages/frontend && npx vite --host` and open in browser.
    Verify:
    1. Dark background fills the viewport
    2. Header is visible at the top with "Eigen Video Editor" on the left
    3. "Subtitles" tab is visible on the right side of the header with green accent (active state)
    4. Below the header, "Drop a video or click to upload" placeholder text is centered
    5. No page reload occurs when clicking the Subtitles tab (it's already active, but the click handler should not cause navigation)
    6. The aesthetic feels professional and dark — no bright colors, no playful elements
  </verify>
  <done>
    Header shows "Eigen Video Editor" left + "Subtitles" tab right. SubtitlesPage shows centered placeholder. Dark theme with grey + green accents applied throughout. Tab navigation works without page reload.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` in packages/frontend starts Vite on 0.0.0.0:5173
2. Browser shows dark-themed app shell
3. Header: "Eigen Video Editor" left, "Subtitles" tab right with green active indicator
4. Content area: centered "Drop a video or click to upload" text
5. No page reloads on tab interaction
6. All colors use CSS custom properties (no hard-coded hex in components)
7. Vite proxy configured to forward /api/* to localhost:3001
</verification>

<success_criteria>
- Vite dev server runs on 0.0.0.0:5173 with API proxy to backend
- Dark theme with DaVinci Resolve aesthetic: dark greys, green accents, no bright colors
- Header layout: app name left, tabs right, fixed at top
- Single "Subtitles" tab — no placeholder tabs, no coming-soon slots
- SubtitlesPage renders centered placeholder content
- Tab switching works without page reload
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
