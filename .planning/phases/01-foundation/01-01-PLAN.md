---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.base.json
  - tsconfig.build.json
  - .gitignore
  - .nvmrc
  - packages/shared-types/package.json
  - packages/shared-types/tsconfig.json
  - packages/shared-types/src/index.ts
  - packages/backend/package.json
  - packages/backend/tsconfig.json
  - packages/frontend/package.json
  - packages/frontend/tsconfig.json
  - packages/remotion-composition/package.json
  - packages/remotion-composition/tsconfig.json
  - packages/remotion-composition/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Running `npm install` at the repo root installs all workspace dependencies"
    - "Running `tsc --build tsconfig.build.json` compiles all packages without errors"
    - "Importing `@eigen/shared-types` from backend or frontend resolves to the shared-types package"
    - "The `data/` folder is gitignored"
  artifacts:
    - path: "package.json"
      provides: "npm workspaces root config"
      contains: "workspaces"
    - path: "tsconfig.base.json"
      provides: "Shared TS compiler options"
      contains: "composite"
    - path: "tsconfig.build.json"
      provides: "Root build references"
      contains: "references"
    - path: "packages/shared-types/src/index.ts"
      provides: "Job, JobStatus, VideoMetadata types"
      exports: ["Job", "JobStatus", "VideoMetadata"]
    - path: "packages/backend/package.json"
      provides: "Backend package definition"
      contains: "@eigen/backend"
    - path: "packages/frontend/package.json"
      provides: "Frontend package definition"
      contains: "@eigen/frontend"
    - path: "packages/remotion-composition/package.json"
      provides: "Remotion composition stub package"
      contains: "@eigen/remotion-composition"
  key_links:
    - from: "packages/backend/tsconfig.json"
      to: "packages/shared-types"
      via: "TypeScript project references"
      pattern: "references.*shared-types"
    - from: "packages/frontend/tsconfig.json"
      to: "packages/shared-types"
      via: "TypeScript project references"
      pattern: "references.*shared-types"
    - from: "package.json"
      to: "packages/*"
      via: "npm workspaces"
      pattern: "workspaces.*packages"
---

<objective>
Scaffold the npm workspaces monorepo with four packages (@eigen/frontend, @eigen/backend, @eigen/remotion-composition, @eigen/shared-types), TypeScript project references, and shared type definitions.

Purpose: Every subsequent plan depends on the monorepo structure, workspace linking, and shared types being in place. This must land first.
Output: A working monorepo where `npm install` succeeds and `tsc --build` compiles cleanly across all packages.
</objective>

<execution_context>
@/Users/eigenair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eigenair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root config and all four package scaffolds</name>
  <files>
    package.json
    tsconfig.base.json
    tsconfig.build.json
    .gitignore
    .nvmrc
    packages/shared-types/package.json
    packages/shared-types/tsconfig.json
    packages/backend/package.json
    packages/backend/tsconfig.json
    packages/frontend/package.json
    packages/frontend/tsconfig.json
    packages/remotion-composition/package.json
    packages/remotion-composition/tsconfig.json
  </files>
  <action>
    Create the monorepo root and all four workspace packages per user decision: `@eigen/frontend`, `@eigen/backend`, `@eigen/remotion-composition`, `@eigen/shared-types`.

    **Root `package.json`:**
    - `"name": "eigen-video-editor"`, `"private": true`
    - `"workspaces": ["packages/*"]`
    - No root-level dependencies — all deps live in individual packages

    **Root `tsconfig.base.json`:**
    - `target: "ES2022"`, `module: "NodeNext"`, `moduleResolution: "NodeNext"`
    - `composite: true`, `declaration: true`, `declarationMap: true`
    - `strict: true`, `skipLibCheck: true`

    **Root `tsconfig.build.json`:**
    - `files: []` (no files to compile at root)
    - `references` pointing to all 4 packages

    **Per-package `package.json` files:**
    - `@eigen/shared-types`: `"type": "module"`, `"main": "dist/index.js"`, `"types": "dist/index.d.ts"`, `"exports"` field pointing to dist. Dev dep: `typescript`.
    - `@eigen/backend`: `"type": "module"`, deps: `fastify`, `@fastify/multipart`, `@fastify/cors`, `@fastify/sse`, `@fastify/static`, `uuid`. Dev deps: `typescript`, `@types/node`, `@types/uuid`. Dependency on `@eigen/shared-types: "workspace:*"`. Add `"scripts": { "dev": "node --watch --experimental-strip-types src/index.ts", "build": "tsc --build" }`.
    - `@eigen/frontend`: `"type": "module"`, deps: `react`, `react-dom`, `react-dropzone`. Dev deps: `vite`, `@vitejs/plugin-react`, `typescript`, `@types/react`, `@types/react-dom`. Dependency on `@eigen/shared-types: "workspace:*"`. Add `"scripts": { "dev": "vite", "build": "vite build" }`.
    - `@eigen/remotion-composition`: `"type": "module"`, `"main": "dist/index.js"`, `"types": "dist/index.d.ts"`. Dev dep: `typescript`. Dependency on `@eigen/shared-types: "workspace:*"`.

    **Per-package `tsconfig.json` files:**
    - All extend `../../tsconfig.base.json`
    - All set `rootDir: "src"`, `outDir: "dist"`
    - Backend, frontend, and remotion-composition all reference `../shared-types`
    - Frontend tsconfig: also add `"jsx": "react-jsx"` and set `module: "ESNext"`, `moduleResolution: "bundler"` (required for Vite — Vite uses its own bundler resolution, not NodeNext)

    **`.gitignore`:**
    - `node_modules/`, `dist/`, `data/`, `.DS_Store`, `*.tsbuildinfo`

    **`.nvmrc`:**
    - Set to Node 22 (LTS)

    Create placeholder `src/` directories for backend and frontend (empty `src/` dirs with a `.gitkeep` if needed — actual source files come in later plans).
  </action>
  <verify>
    Run `npm install` at the repo root — must complete without errors and create symlinks in `node_modules/@eigen/` for all four packages.
    Run `ls node_modules/@eigen/` — should show `backend`, `frontend`, `shared-types`, `remotion-composition`.
  </verify>
  <done>
    All four workspace packages exist with correct names, dependencies, and TypeScript configs. `npm install` succeeds. Workspace linking is verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shared types and remotion-composition stub, verify cross-package compilation</name>
  <files>
    packages/shared-types/src/index.ts
    packages/remotion-composition/src/index.ts
  </files>
  <action>
    **`packages/shared-types/src/index.ts`:**
    Define the core types that all packages will use in Phase 1:

    ```typescript
    export type JobStatus = 'uploading' | 'normalizing' | 'ready' | 'failed'

    export interface VideoMetadata {
      duration: number     // seconds
      fps: number
      width: number
      height: number
      codec: string
    }

    export interface Job {
      id: string
      status: JobStatus
      progress: number         // 0-100
      originalFilename?: string
      metadata?: VideoMetadata
      thumbnailPath?: string
      error?: string
      createdAt: number        // Date.now()
    }
    ```

    **`packages/remotion-composition/src/index.ts`:**
    Create a minimal stub that exports a placeholder. This package is scaffolded now but built out in Phase 3.

    ```typescript
    // Remotion composition — built in Phase 3
    // Stub export to satisfy TypeScript project references
    export const COMPOSITION_ID = 'SubtitleComposition'
    ```

    After writing these files, run `tsc --build tsconfig.build.json` to verify the entire monorepo compiles. Fix any errors.

    Then verify cross-package imports work by temporarily checking that the backend and remotion-composition tsconfigs can resolve `@eigen/shared-types` types (the build should succeed with the project references in place).
  </action>
  <verify>
    Run `tsc --build tsconfig.build.json` at the repo root — must complete with zero errors.
    Verify `packages/shared-types/dist/index.js` and `packages/shared-types/dist/index.d.ts` exist after build.
    Verify `packages/remotion-composition/dist/index.js` exists after build.
  </verify>
  <done>
    `Job`, `JobStatus`, and `VideoMetadata` types are exported from `@eigen/shared-types`. `tsc --build` compiles the full monorepo cleanly. Cross-package TypeScript references resolve correctly.
  </done>
</task>

</tasks>

<verification>
1. `npm install` at repo root succeeds
2. `ls node_modules/@eigen/` shows all 4 packages
3. `tsc --build tsconfig.build.json` compiles with zero errors
4. `packages/shared-types/dist/index.d.ts` exports `Job`, `JobStatus`, `VideoMetadata`
5. `data/` directory pattern is in `.gitignore`
6. `git status` shows no untracked `data/` or `node_modules/` or `dist/` directories
</verification>

<success_criteria>
- npm workspaces monorepo with 4 packages resolves and installs correctly
- TypeScript project references compile across all packages
- Shared types (Job, JobStatus, VideoMetadata) are defined and importable
- .gitignore covers node_modules, dist, data, .DS_Store, tsbuildinfo
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
