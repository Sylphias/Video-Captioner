---
phase: 07-text-animation-creator
plan: 05
type: execute
wave: 4
depends_on: ["07-03", "07-04"]
files_modified:
  - packages/frontend/src/components/StageTabBar.tsx
  - packages/frontend/src/components/StageTabBar.css
  - packages/frontend/src/pages/SubtitlesPage.tsx
  - packages/frontend/src/pages/SubtitlesPage.css
autonomous: false

must_haves:
  truths:
    - "User can navigate to an Animation stage tab alongside Timing and Text"
    - "Animation stage renders the full AnimationEditor with preset browsing and editing"
    - "Selecting a preset as global default immediately applies it to the video preview"
    - "Per-phrase animation overrides are visible in the video preview"
    - "User can create, edit, save, duplicate, and delete presets through the Animation tab"
    - "Stage transitions preserve animation state (no data loss navigating between tabs)"
  artifacts:
    - path: "packages/frontend/src/components/StageTabBar.tsx"
      provides: "Updated StageId union with 'animation' and STAGES array with Animation tab"
      contains: "animation"
    - path: "packages/frontend/src/pages/SubtitlesPage.tsx"
      provides: "Animation stage rendering with AnimationEditor component"
      contains: "AnimationEditor"
  key_links:
    - from: "packages/frontend/src/pages/SubtitlesPage.tsx"
      to: "packages/frontend/src/components/AnimationEditor/AnimationEditor.tsx"
      via: "Conditional rendering when activeStage === 'animation'"
      pattern: "activeStage.*animation.*AnimationEditor"
    - from: "packages/frontend/src/components/StageTabBar.tsx"
      to: "packages/frontend/src/pages/SubtitlesPage.tsx"
      via: "StageId type includes 'animation', onStageChange callback"
      pattern: "animation"
---

<objective>
Integrate the Animation Editor into the editing workflow as a new stage tab and verify the full animation pipeline end-to-end.

Purpose: All building blocks are ready — types (Plan 01), backend (Plan 02), store/hook/preview wiring (Plan 03), and editor UI (Plan 04). This plan adds the "Animation" tab to the stage navigation, renders the AnimationEditor in SubtitlesPage, and includes a human verification checkpoint for the complete workflow.

Output: "Animation" tab in StageTabBar, AnimationEditor rendered in SubtitlesPage, full end-to-end animation workflow verified by user.
</objective>

<execution_context>
@/Users/eigenair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eigenair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-text-animation-creator/07-RESEARCH.md
@.planning/phases/07-text-animation-creator/07-CONTEXT.md
@.planning/phases/07-text-animation-creator/07-01-SUMMARY.md
@.planning/phases/07-text-animation-creator/07-02-SUMMARY.md
@.planning/phases/07-text-animation-creator/07-03-SUMMARY.md
@.planning/phases/07-text-animation-creator/07-04-SUMMARY.md
@packages/frontend/src/components/StageTabBar.tsx
@packages/frontend/src/pages/SubtitlesPage.tsx
@packages/frontend/src/pages/SubtitlesPage.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Animation stage tab and wire AnimationEditor into SubtitlesPage</name>
  <files>
    packages/frontend/src/components/StageTabBar.tsx
    packages/frontend/src/components/StageTabBar.css
    packages/frontend/src/pages/SubtitlesPage.tsx
    packages/frontend/src/pages/SubtitlesPage.css
  </files>
  <action>
    **In packages/frontend/src/components/StageTabBar.tsx:**

    1. Update the `StageId` type to include 'animation':
       ```typescript
       export type StageId = 'timing' | 'text' | 'animation'
       ```

    2. Add the Animation stage to the STAGES array:
       ```typescript
       const STAGES: Stage[] = [
         { id: 'timing', label: 'Timeline View' },
         { id: 'text', label: 'Text Edit View' },
         { id: 'animation', label: 'Animation' },
       ]
       ```

    3. No other changes needed to StageTabBar — the existing rendering logic handles the new entry automatically.

    **In packages/frontend/src/pages/SubtitlesPage.tsx:**

    1. Add import for AnimationEditor:
       ```typescript
       import { AnimationEditor } from '../components/AnimationEditor/AnimationEditor.tsx'
       ```

    2. In the editor section (inside `subtitles-page__editor-section`), add the animation stage rendering alongside the existing timing and text stages:
       ```tsx
       {activeStage === 'animation' && (
         <AnimationEditor />
       )}
       ```

       Place this after the `activeStage === 'timing'` block and before the closing `</div>` of editor-section.

    3. The AnimationEditor component manages its own state internally (preset selection, editing params, etc.), so no additional props are needed from SubtitlesPage.

    **In packages/frontend/src/pages/SubtitlesPage.css:**

    The AnimationEditor needs the full height of the editor section. Ensure the `.subtitles-page__editor-section` allows its children to fill available height. Add if not already present:
    ```css
    .subtitles-page__editor-section {
      flex: 1;
      min-height: 0;
      overflow: hidden;
    }
    ```

    Also ensure the AnimationEditor grid layout works within the section:
    ```css
    .subtitles-page__editor-section > .animation-editor {
      height: 100%;
    }
    ```

    **Verify the stage transition toast logic:** The existing `handleStageChange` function shows a toast when transitioning FROM 'text' stage. The animation stage does not need a similar transition toast — no special handling needed. But verify that navigating to/from 'animation' does not break the existing toast logic (it shouldn't since the check is `activeStage === 'text'`).
  </action>
  <verify>
    Run `npx tsc --build --force` — zero errors. Start dev servers. Open the app with a transcribed video. Verify:
    1. Three tabs visible in StageTabBar: "Timeline View", "Text Edit View", "Animation".
    2. Clicking "Animation" tab shows the AnimationEditor.
    3. PresetList shows 7 built-in presets.
    4. Selecting a preset shows its animation in the AnimationPreview.
    5. Clicking "Set as default" on a preset makes the main video preview show that animation.
    6. Navigating back to Timeline/Text tabs and returning to Animation preserves the selected preset.
  </verify>
  <done>
    StageTabBar includes "Animation" tab. SubtitlesPage renders AnimationEditor when animation stage is active. Full editor section layout works with proper height. Stage transitions between all three tabs work without data loss.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete animation workflow end-to-end</name>
  <files>packages/frontend/src/pages/SubtitlesPage.tsx</files>
  <action>
    CHECKPOINT: Human verification of the full text animation creator system built across Plans 01-05.

    What was built:
    - Animation type system with enter/active/exit phases (Plan 01)
    - SQLite backend with 7 built-in presets and CRUD API (Plan 02)
    - Animation rendering engine in Remotion composition (Plan 01)
    - Frontend store with preset selection and per-phrase overrides (Plan 03)
    - Animation Editor UI with preset list, phase timeline, parameter panel, and live preview (Plan 04)
    - "Animation" stage tab integrated into the editing workflow (Plan 05 Task 1)

    Present the verification checklist to the user and await confirmation.
  </action>
  <verify>
    User performs the following verification steps:

    **1. Animation Tab Navigation:**
    - Click the "Animation" tab in the stage bar
    - Verify the Animation Editor loads with a preset list on the left

    **2. Preset Browsing:**
    - Verify 7 built-in presets are listed (Classic Fade, Slide Up, Pop In, Word Cascade, Typewriter, Blur Reveal, Jiggle Pop)
    - Type in the search box to filter presets
    - Click different presets and observe the AnimationPreview updating

    **3. Animation Preview:**
    - Select "Classic Fade" — sample text should fade in
    - Select "Slide Up" — sample text should slide up from below
    - Select "Pop In" — sample text should pop/scale in with slight bounce
    - Select "Word Cascade" — words should appear one by one with stagger
    - Select "Typewriter" — text should appear character by character

    **4. Parameter Editing:**
    - Select a preset, click the "Enter" block in the phase timeline
    - Change the animation type in the PhasePanel dropdown
    - Adjust the duration slider — preview should update after ~300ms
    - Click the "Hold" block — verify active animation controls appear
    - Click the "Exit" block — verify mirror toggle and exit controls

    **5. Phase Timeline Drag:**
    - Drag the handle between Enter and Hold blocks to resize enter duration
    - Drag the handle between Hold and Exit blocks to resize exit duration
    - Verify the blocks resize proportionally

    **6. Apply to Video:**
    - Click "Set as default" on a preset (e.g., "Slide Up")
    - Switch to Timeline View tab and play the video
    - Verify subtitle phrases animate with the selected preset in the main video preview

    **7. Preset CRUD:**
    - Click "New Preset" — verify a new preset is created and selected
    - Edit its parameters and click "Save"
    - Duplicate a built-in preset — verify a copy appears
    - Delete a custom preset — verify it's removed
    - Try to delete a built-in preset — verify it's protected (cannot delete)

    **8. Per-Phrase Override:**
    - Switch to Timeline View, click the edit (pencil) button on a phrase to open StyleDrawer
    - In the phrase StyleDrawer, find the "Animation Preset" dropdown
    - Select a different preset for that specific phrase
    - Play the video and verify that specific phrase uses the override while others use the global default

    **9. Persistence:**
    - Restart the backend server
    - Refresh the browser
    - Verify custom presets are still listed (SQLite persistence)

    **10. Undo/Redo:**
    - Set a global animation preset, then Cmd+Z — verify it reverts
    - Set a per-phrase override, then Cmd+Z — verify it reverts
  </verify>
  <done>
    User has approved the complete animation workflow. All Phase 7 success criteria met: custom animation creation, preset storage, resolution independence, cross-session persistence, and preset application to subtitles. Type "approved" or describe issues to fix.
  </done>
</task>

</tasks>

<verification>
- Animation tab visible and navigable
- All 7 built-in presets render correctly in preview
- Preset CRUD works (create, edit, duplicate, delete)
- Global default animation reflects in video preview
- Per-phrase animation override works
- Phase timeline drag adjusts durations
- Parameter changes debounce correctly to preview
- Presets persist across server restart
- Undo/redo covers animation preset changes
</verification>

<success_criteria>
Phase 7 success criteria met:
1. User can create custom text animations with configurable parameters (enter/exit transitions, timing, easing) -- verified via Animation Editor
2. Animations can be saved as reusable presets with descriptive names -- verified via preset CRUD
3. Presets support both vertical (9:16) and horizontal (16:9) video resolutions -- verified via resolution-independent fractions
4. Saved presets persist across sessions -- verified via SQLite + server restart
5. User can apply a saved animation preset to the current project's subtitles -- verified via "Set as default" + video preview
</success_criteria>

<output>
After completion, create `.planning/phases/07-text-animation-creator/07-05-SUMMARY.md`
</output>
